import { defineComponent, ref, watch, computed, unref, mergeProps, resolveComponent, withCtx, createVNode, toDisplayString, openBlock, createBlock, Fragment, renderList, createTextVNode, createCommentVNode, withModifiers, withKeys, h, nextTick, useSSRContext, shallowRef, getCurrentInstance, provide, cloneVNode, createElementBlock } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/vue/index.mjs';
import { ssrRenderStyle, ssrRenderComponent, ssrInterpolate, ssrRenderList, ssrRenderClass, ssrRenderAttr, ssrIncludeBooleanAttr, ssrRenderTeleport, ssrRenderAttrs } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/vue/server-renderer/index.mjs';
import { message } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/ant-design-vue/lib/index.js';
import { _ as _export_sfc, b as useRoute, a as useRequestEvent, u as useNuxtApp } from './server.mjs';
import { getRequestURL } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/h3/dist/index.mjs';
import { G as GetDynamicFormHeaders } from './DynamicFormHeaderApi-B0em2sUt.mjs';
import { l as logoImage } from './logo-C8qnEBYq.mjs';
import { SearchOutlined, DeleteOutlined } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/@ant-design/icons-vue/lib/index.js';
import { debounce } from 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/lodash-es/lodash.js';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/ofetch/dist/node.mjs';
import '../_/renderer.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/vue-bundle-renderer/dist/runtime.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/ufo/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/destr/dist/index.mjs';
import '../_/nitro.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/hookable/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/node-mock-http/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unstorage/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unstorage/drivers/fs.mjs';
import 'file:///C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/@nuxt/nitro-server/dist/runtime/utils/cache-driver.js';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unstorage/drivers/fs-lite.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/ohash/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/klona/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/defu/dist/defu.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/scule/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/radix3/dist/index.mjs';
import 'node:fs';
import 'node:url';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/pathe/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unhead/dist/server.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/devalue/index.js';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unhead/dist/utils.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/unctx/dist/index.mjs';
import 'file://C:/XNKT/FE_XNKT-V2/FE_XNKT/node_modules/vue-router/vue-router.node.mjs';

const _sfc_main$c = /* @__PURE__ */ defineComponent({
  __name: "Loading",
  __ssrInlineRender: true,
  props: {
    spinning: { type: Boolean },
    fullscreen: { type: Boolean, default: false }
  },
  setup(__props) {
    return (_ctx, _push, _parent, _attrs) => {
      if (__props.spinning && __props.fullscreen) {
        ssrRenderTeleport(_push, (_push2) => {
          _push2(`<div class="fixed inset-0 z-[9999] flex items-center justify-center bg-black/20"><div class="h-12 w-12 animate-spin rounded-full border-4 border-white/60 border-t-white"></div></div>`);
        }, "body", false, _parent);
      } else if (__props.spinning) {
        _push(`<div${ssrRenderAttrs(mergeProps({ class: "flex items-center justify-center py-8" }, _attrs))}><div class="h-10 w-10 animate-spin rounded-full border-4 border-gray-300 border-t-blue-600"></div></div>`);
      } else {
        _push(`<!---->`);
      }
    };
  }
});
const _sfc_setup$c = _sfc_main$c.setup;
_sfc_main$c.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/Loading.vue");
  return _sfc_setup$c ? _sfc_setup$c(props, ctx) : void 0;
};
const __nuxt_component_0 = Object.assign(_sfc_main$c, { __name: "Loading" });
const _sfc_main$b = /* @__PURE__ */ defineComponent({
  __name: "ErrorView",
  __ssrInlineRender: true,
  props: {
    message: {}
  },
  setup(__props) {
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<div${ssrRenderAttrs(mergeProps({ class: "w-full p-6" }, _attrs))}><div class="mx-auto max-w-3xl rounded-lg border border-red-200 bg-red-50 p-4 text-red-800"><div class="text-base font-semibold">Có lỗi xảy ra</div><div class="mt-2 whitespace-pre-wrap text-sm">${ssrInterpolate(__props.message)}</div></div></div>`);
    };
  }
});
const _sfc_setup$b = _sfc_main$b.setup;
_sfc_main$b.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/ErrorView.vue");
  return _sfc_setup$b ? _sfc_setup$b(props, ctx) : void 0;
};
const __nuxt_component_1 = Object.assign(_sfc_main$b, { __name: "ErrorView" });
const _sfc_main$a = /* @__PURE__ */ defineComponent({
  __name: "NotificationToast",
  __ssrInlineRender: true,
  props: {
    show: { type: Boolean },
    message: {},
    duration: { default: 2e3 }
  },
  emits: ["hide"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    watch(
      () => props.show,
      (show) => {
        if (!show) return;
        message.info(props.message, props.duration / 1e3);
        (void 0).setTimeout(() => emit("hide"), props.duration);
      }
    );
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<span${ssrRenderAttrs(_attrs)}></span>`);
    };
  }
});
const _sfc_setup$a = _sfc_main$a.setup;
_sfc_main$a.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/NotificationToast.vue");
  return _sfc_setup$a ? _sfc_setup$a(props, ctx) : void 0;
};
const __nuxt_component_2 = Object.assign(_sfc_main$a, { __name: "NotificationToast" });
const clientOnlySymbol = /* @__PURE__ */ Symbol.for("nuxt:client-only");
defineComponent({
  name: "ClientOnly",
  inheritAttrs: false,
  props: ["fallback", "placeholder", "placeholderTag", "fallbackTag"],
  ...false,
  setup(props, { slots, attrs }) {
    const mounted = shallowRef(false);
    const vm = getCurrentInstance();
    if (vm) {
      vm._nuxtClientOnly = true;
    }
    provide(clientOnlySymbol, true);
    return () => {
      if (mounted.value) {
        const vnodes = slots.default?.();
        if (vnodes && vnodes.length === 1) {
          return [cloneVNode(vnodes[0], attrs)];
        }
        return vnodes;
      }
      const slot = slots.fallback || slots.placeholder;
      if (slot) {
        return h(slot);
      }
      const fallbackStr = props.fallback || props.placeholder || "";
      const fallbackTag = props.fallbackTag || props.placeholderTag || "span";
      return createElementBlock(fallbackTag, attrs, fallbackStr);
    };
  }
});
function useRequestURL(opts) {
  {
    return getRequestURL(useRequestEvent(), opts);
  }
}
const pickQueryValue = (value) => {
  if (Array.isArray(value)) {
    return (value[0] ?? "").toString().trim();
  }
  if (value === null || value === void 0) {
    return "";
  }
  return String(value).trim();
};
const pickWoFromQueryString = (queryString) => {
  const params = new URLSearchParams(queryString || "");
  return (params.get("wocode") || "").trim() || (params.get("wo") || "").trim() || (params.get("WO") || "").trim();
};
const resolveWoCode = (wo) => {
  const direct = (wo || "").trim();
  if (direct) return direct;
  const nuxtApp = useNuxtApp();
  const fromRouterCurrentRoute = pickQueryValue(nuxtApp.$router?.currentRoute?.value?.query?.wocode) || pickQueryValue(nuxtApp.$router?.currentRoute?.value?.query?.wo) || pickQueryValue(nuxtApp.$router?.currentRoute?.value?.query?.WO);
  if (fromRouterCurrentRoute) return fromRouterCurrentRoute;
  const route = useRoute();
  const fromRoute = pickQueryValue(route.query.wocode) || pickQueryValue(route.query.wo) || pickQueryValue(route.query.WO);
  if (fromRoute) return fromRoute;
  const fromRouteFullPath = pickWoFromQueryString(route.fullPath.split("?")[1] || "");
  if (fromRouteFullPath) return fromRouteFullPath;
  const requestUrl = useRequestURL();
  const fromRequest = pickWoFromQueryString(requestUrl.search);
  return fromRequest;
};
const resolveWoCodeWithRetry = async (wo) => {
  let resolvedWo = resolveWoCode(wo);
  if (resolvedWo) return resolvedWo;
  return "";
};
async function GetFirstForm(wo) {
  const { $axios } = useNuxtApp();
  try {
    const resolvedWo = await resolveWoCodeWithRetry(wo);
    if (!resolvedWo) {
      throw new Error("Missing wo/wocode in URL query after retry");
    }
    const response = await $axios.get(`FirstForm/FirstForm`, {
      params: { wo: resolvedWo }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function GetToolByWO(wo) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.get(`Tool/GetToolByWO`, {
      params: { wo }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function AddTool(wo, toolData) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.post(`Tool/AddTool`, {
      Ma_Tool: toolData.Ma_Tool,
      Ten_TB: toolData.Ten_TB,
      Ma_Ky_Hieu: toolData.Ma_Ky_Hieu,
      So_Seri: toolData.So_Seri,
      Nha_SX: toolData.Nha_SX,
      Hang_Kiem_Dinh: toolData.Hang_Kiem_Dinh
    }, {
      params: { wo }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function DeleteTool(wo, ma_Tool) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.delete(`Tool/DeleteTool`, {
      params: { wo, ma_Tool }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
const GetToolsPaged = async (params) => {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.get("Tool/GetToolsPaged", {
      params: {
        currentPage: params.currentPage,
        pageSize: params.pageSize,
        gian: params.gian,
        ...params.search && { search: params.search },
        ...params.bophan && { bophan: params.bophan }
      }
    });
    return response.data;
  } catch (error) {
    console.error("Error in GetToolsPaged API:", error);
    throw error;
  }
};
async function UpdateTichTruocND(wo, line, istick) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`FORMMAIN/UpdateTichTruocND`, null, {
      params: { wo, line, istick }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function UpdateCVK2(wo, line, content) {
  const { $axios } = useNuxtApp();
  try {
    const params = { wo, line, content };
    const response = await $axios.put(`FORMMAIN/NoteCVK2`, null, {
      params
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function UpdateTichBaoDuongND(wo, line, istick) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`FORMMAIN/TichNdBaoDuong`, null, {
      params: { wo, line, istick }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function NoteCVK4(wo, content) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`FORMMAIN/NoteCVK4`, null, {
      params: { wo, content }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function NoteCVK6(wo, content) {
  const { $axios } = useNuxtApp();
  try {
    const params = { wo, content };
    const response = await $axios.put(`FORMMAIN/NoteCVK6`, null, {
      params
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function UpdateTichCVK(wo, line, istick, type) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`U5NDCV/UpdateTichCVK`, null, {
      params: {
        wo,
        line,
        istick,
        type
      }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
async function TichNdSau(wo, line, istick) {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`FORMMAIN/TichNdSau`, null, {
      params: { wo, line, istick }
    });
    return response.data;
  } catch (error) {
    throw error;
  }
}
const GetNguoiChoPhep = async (org, startrow = 1) => {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.get("Part/NguoiChoPhep", {
      params: { org, startrow },
      __skipWoInject: true
    });
    return response.data;
  } catch (error) {
    console.error("Error fetching NguoiChoPhep:", error);
    throw error;
  }
};
const UpdateNguoiChoPhep = async (wo, nguoiChoPhep) => {
  const { $axios } = useNuxtApp();
  try {
    const response = await $axios.put(`/Person/UpdateNguoiChoPhep?wo=${wo}&nguoiChoPhep=${nguoiChoPhep}`);
    return response.data;
  } catch (error) {
    console.error("Error updating người cho phép:", error);
    throw error;
  }
};
const pageSize = 50;
const _sfc_main$9 = /* @__PURE__ */ defineComponent({
  __name: "HeaderPermission",
  __ssrInlineRender: true,
  props: {
    data: {},
    wocode: {},
    disabled: { type: Boolean, default: false },
    dynamicHeaderLines: {}
  },
  emits: ["updated"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const personList = ref([]);
    const selectedPerson = ref("");
    const selectedJobTitle = ref("");
    const personSearchValue = ref("");
    const isLoadingAllPersons = ref(false);
    const personLoadRequestId = ref(0);
    const headerTitleLines = computed(
      () => (props.dynamicHeaderLines || []).map((line) => String(line || "").trim()).filter(Boolean).slice(0, 4)
    );
    const handlePersonSearch = (value) => {
      personSearchValue.value = value;
    };
    const filteredPersons = computed(() => {
      if (!personSearchValue.value) return personList.value;
      return personList.value.filter(
        (person) => person.per_desc?.toLowerCase().includes(personSearchValue.value.toLowerCase())
      );
    });
    const parseJobTitle = (desc) => {
      const match = desc.match(/\(([^)]+)\)/);
      return match && match[1] ? match[1].trim() : "";
    };
    const stripJobTitle = (desc) => {
      return (desc || "").replace(/\s*\([^)]*\)\s*$/, "").trim();
    };
    const syncSelectedJobTitle = (personCode) => {
      if (!personCode) {
        selectedJobTitle.value = "";
        return;
      }
      const selectedPersonData = personList.value.find((p) => p.per_code === personCode);
      selectedJobTitle.value = selectedPersonData?.per_jobtitle || "";
    };
    const mergeUniqueByCode = (current, incoming) => {
      const map = /* @__PURE__ */ new Map();
      for (const item of current) map.set(item.per_code, item);
      for (const item of incoming) map.set(item.per_code, item);
      return Array.from(map.values());
    };
    const normalizeNguoiChoPhepRows = (payload) => {
      if (Array.isArray(payload)) return payload;
      if (Array.isArray(payload?.data)) return payload.data;
      if (Array.isArray(payload?.Data)) return payload.Data;
      return [];
    };
    const fetchPersons = async (org, startrowValue, append) => {
      try {
        const res = await GetNguoiChoPhep(org, startrowValue);
        const mapped = normalizeNguoiChoPhepRows(res).map((item) => ({
          per_code: item.schedgroup || item.userdefinedfieldvalue,
          per_desc: item.schedgroupdesc || item.description,
          per_jobtitle: item.JobTitle?.trim() || parseJobTitle(item.schedgroupdesc || item.description),
          per_trade: "",
          per_udfchkbox01: ""
        }));
        personList.value = append ? mergeUniqueByCode(personList.value, mapped) : mapped;
        syncSelectedJobTitle(selectedPerson.value);
        return mapped.length;
      } catch (error) {
        console.error("Failed to fetch NguoiChoPhep:", error);
        return 0;
      }
    };
    const loadAllPersons = async (org) => {
      if (isLoadingAllPersons.value) return;
      const requestId = ++personLoadRequestId.value;
      isLoadingAllPersons.value = true;
      personList.value = [];
      try {
        let currentStartRow = 1;
        let pageCalls = 0;
        const maxPageCalls = 300;
        while (pageCalls < maxPageCalls) {
          const loadedCount = await fetchPersons(org, currentStartRow, currentStartRow > 1);
          if (requestId !== personLoadRequestId.value) return;
          if (loadedCount <= 0) {
            break;
          }
          currentStartRow += pageSize;
          pageCalls += 1;
        }
        if (pageCalls >= maxPageCalls) {
          console.warn("NguoiChoPhep paging reached safety limit, stopping prefetch.");
        }
      } finally {
        if (requestId === personLoadRequestId.value) {
          isLoadingAllPersons.value = false;
        }
      }
    };
    const handlePersonChange = async (value) => {
      if (props.disabled) return;
      const personCode = value;
      selectedPerson.value = personCode || "";
      syncSelectedJobTitle(selectedPerson.value);
      if (!personCode) return;
      try {
        await UpdateNguoiChoPhep(props.wocode, personCode);
        emit("updated");
      } catch (error) {
        console.error("Failed to update người cho phép:", error);
      }
    };
    watch(
      () => props.data?.NguoiChoPhep,
      (newValue) => {
        selectedPerson.value = newValue || "";
        syncSelectedJobTitle(selectedPerson.value);
      },
      { immediate: true }
    );
    watch(
      () => props.data?.gian,
      (newOrg) => {
        const org = (newOrg || "").trim();
        if (!org) return;
        loadAllPersons(org);
      },
      { immediate: true }
    );
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_select = resolveComponent("a-select");
      const _component_a_select_option = resolveComponent("a-select-option");
      if (__props.data) {
        _push(`<div${ssrRenderAttrs(_attrs)} data-v-0b0d6043><div class="flex w-full h-[150px] border-b-2 border-black" data-v-0b0d6043><div class="flex justify-center items-center px-2 border-r-2 border-black" data-v-0b0d6043><img${ssrRenderAttr("src", logoImage)} alt="Logo" class="h-full w-auto object-contain" data-v-0b0d6043></div><div class="flex flex-[4] justify-center items-center min-w-[50%] border-r-2 border-black" data-v-0b0d6043><div class="text-center font-black text-xl" data-v-0b0d6043>`);
        if (headerTitleLines.value.length) {
          _push(`<!--[-->`);
          ssrRenderList(headerTitleLines.value, (line, idx) => {
            _push(`<div data-v-0b0d6043>${ssrInterpolate(line)}</div>`);
          });
          _push(`<!--]-->`);
        } else {
          _push(`<!--[--> PHIẾU BẢO DƯỠNG VÀ NGHIỆM THU<br data-v-0b0d6043> HỆ THỐNG ĐIỆN<br data-v-0b0d6043> MAINTENANCE AND CHECKLIST<br data-v-0b0d6043> FOR ELECTRIC SYSTEM <!--]-->`);
        }
        _push(`</div></div><div class="flex flex-[1.1] justify-start items-center pl-3 md:pl-4 border-black" data-v-0b0d6043><div class="text-left font-bold text-base md:text-lg leading-tight" data-v-0b0d6043><div data-v-0b0d6043>Giàn / Объект: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.gian || "")}</span></div><div data-v-0b0d6043>Ngày / Date: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.evt_TGBD || "")}</span></div><div data-v-0b0d6043>Số / номер: <span class="font-normal" data-v-0b0d6043></span></div><div data-v-0b0d6043>Page:</div></div></div></div><div class="flex w-full min-h-[120px] mt-2 flex-wrap md:flex-nowrap" data-v-0b0d6043><div class="flex flex-1 min-w-[280px] md:min-w-0 justify-start items-start py-2 pl-4" data-v-0b0d6043><div class="text-left font-bold text-lg" data-v-0b0d6043><div data-v-0b0d6043>Tên hệ thống: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.TENTBI || "")}</span></div><div data-v-0b0d6043>Наим. сист.</div><div data-v-0b0d6043>Dạng bảo dưỡng: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.DangBaoDuong || "")}</span></div><div data-v-0b0d6043>Вид ППР</div></div></div><div class="flex flex-1 min-w-[280px] md:min-w-0 justify-start items-start py-2 pl-4" data-v-0b0d6043><div class="text-left font-bold text-lg" data-v-0b0d6043><div data-v-0b0d6043>Ký mã hiệu: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.KyMaHieu || "")}</span></div><div data-v-0b0d6043>Тип, марка</div><div data-v-0b0d6043>Theo hướng dẫn số: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.TheoHuongDan || "")}</span></div><div data-v-0b0d6043>По инструкции №</div></div></div><div class="flex flex-1 min-w-[220px] md:min-w-0 justify-start items-start py-2 pl-4" data-v-0b0d6043><div class="text-left font-bold text-lg" data-v-0b0d6043><div data-v-0b0d6043>Vị trí lắp đặt: <span class="font-normal" data-v-0b0d6043>${ssrInterpolate(__props.data.ViTriLapDat || "")}</span></div><div data-v-0b0d6043>Место установки</div></div></div></div><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm" data-v-0b0d6043> 1. Cho phép đưa hệ thống vào bảo dưỡng / Разрешение ввода в ППР </div><div class="flex w-full mt-4 pb-4 min-h-[80px]" data-v-0b0d6043><div class="flex flex-1 justify-start items-center pl-6 py-2" data-v-0b0d6043><div class="flex items-center gap-6" data-v-0b0d6043><div class="text-left font-bold min-w-[140px] text-sm leading-relaxed" data-v-0b0d6043> Người cho phép: <br data-v-0b0d6043> Лицо, разрешающее </div>`);
        _push(ssrRenderComponent(_component_a_select, {
          value: selectedPerson.value,
          "onUpdate:value": ($event) => selectedPerson.value = $event,
          "show-search": "",
          placeholder: "Chọn người cho phép",
          "filter-option": false,
          onSearch: handlePersonSearch,
          onChange: handlePersonChange,
          disabled: __props.disabled,
          class: "custom-select w-[280px] h-[40px]",
          size: "middle",
          optionLabelProp: "label",
          id: "personSelect",
          name: "cbGIANTRUONG"
        }, {
          default: withCtx((_, _push2, _parent2, _scopeId) => {
            if (_push2) {
              _push2(`<!--[-->`);
              ssrRenderList(filteredPersons.value, (person) => {
                _push2(ssrRenderComponent(_component_a_select_option, {
                  key: person.per_code,
                  value: person.per_code,
                  label: stripJobTitle(person.per_desc)
                }, {
                  default: withCtx((_2, _push3, _parent3, _scopeId2) => {
                    if (_push3) {
                      _push3(`<div class="flex flex-col" data-v-0b0d6043${_scopeId2}><span data-v-0b0d6043${_scopeId2}>${ssrInterpolate(stripJobTitle(person.per_desc))}</span><span class="text-xs text-gray-500" data-v-0b0d6043${_scopeId2}>${ssrInterpolate(person.per_jobtitle)}</span></div>`);
                    } else {
                      return [
                        createVNode("div", { class: "flex flex-col" }, [
                          createVNode("span", null, toDisplayString(stripJobTitle(person.per_desc)), 1),
                          createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(person.per_jobtitle), 1)
                        ])
                      ];
                    }
                  }),
                  _: 2
                }, _parent2, _scopeId));
              });
              _push2(`<!--]-->`);
            } else {
              return [
                (openBlock(true), createBlock(Fragment, null, renderList(filteredPersons.value, (person) => {
                  return openBlock(), createBlock(_component_a_select_option, {
                    key: person.per_code,
                    value: person.per_code,
                    label: stripJobTitle(person.per_desc)
                  }, {
                    default: withCtx(() => [
                      createVNode("div", { class: "flex flex-col" }, [
                        createVNode("span", null, toDisplayString(stripJobTitle(person.per_desc)), 1),
                        createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(person.per_jobtitle), 1)
                      ])
                    ]),
                    _: 2
                  }, 1032, ["value", "label"]);
                }), 128))
              ];
            }
          }),
          _: 1
        }, _parent));
        _push(`</div></div><div class="flex flex-1 justify-start items-center pl-6 py-2" data-v-0b0d6043><div class="flex items-center gap-6" data-v-0b0d6043><div class="text-left font-bold min-w-[140px] text-sm leading-relaxed" data-v-0b0d6043> Chức danh: <br data-v-0b0d6043> Должность </div><div class="min-h-[40px] flex items-center px-4 rounded-lg w-[220px] text-sm shadow-sm hover:shadow-md transition-all duration-200" data-v-0b0d6043><span id="jobTitleDisplayJob" class="font-medium" data-v-0b0d6043>${ssrInterpolate(selectedJobTitle.value || "")}</span></div></div></div><div class="flex flex-1 justify-start items-center pl-6 py-2" data-v-0b0d6043><div class="flex items-center gap-6" data-v-0b0d6043><div class="text-left font-bold min-w-[140px] text-sm leading-relaxed" data-v-0b0d6043> Ký tên: <br data-v-0b0d6043> Подпись </div><div class="min-h-[40px] flex items-center px-4 rounded-lg w-[220px] text-sm shadow-sm hover:shadow-md transition-all duration-200" data-v-0b0d6043><span id="signatureDisplay" class="font-medium" data-v-0b0d6043>${(__props.data.NgayNguoiChoPhep || "") ?? ""}</span></div></div></div></div></div>`);
      } else {
        _push(`<!---->`);
      }
    };
  }
});
const _sfc_setup$9 = _sfc_main$9.setup;
_sfc_main$9.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/HeaderPermission.vue");
  return _sfc_setup$9 ? _sfc_setup$9(props, ctx) : void 0;
};
const HeaderPermission = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$9, [["__scopeId", "data-v-0b0d6043"]]), { __name: "HeaderPermission" });
const _sfc_main$8 = /* @__PURE__ */ defineComponent({
  __name: "TextareaField",
  __ssrInlineRender: true,
  props: {
    label: {},
    modelValue: {},
    fieldId: { default: "textarea-field" },
    placeholder: { default: "" },
    rows: { default: 3 },
    width: { default: "600px" },
    height: { default: "100px" }
  },
  emits: ["update:modelValue", "blur"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const textareaClass = computed(() => {
      return `flex-1 border border-gray-300 rounded-md p-2 text-sm resize-y focus:outline-none focus:ring-2 focus:ring-blue-500 w-[${props.width}] h-[${props.height}]`;
    });
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<div${ssrRenderAttrs(mergeProps({ class: "flex items-start w-full max-w-95% mx-auto" }, _attrs))}><label${ssrRenderAttr("for", __props.fieldId)} class="text-base font-medium whitespace-nowrap mr-3 pt-1">${ssrInterpolate(__props.label)}</label><textarea${ssrRenderAttr("id", __props.fieldId)} class="${ssrRenderClass(textareaClass.value)}"${ssrRenderAttr("rows", __props.rows)}${ssrRenderAttr("placeholder", __props.placeholder)}>${ssrInterpolate(__props.modelValue)}</textarea></div>`);
    };
  }
});
const _sfc_setup$8 = _sfc_main$8.setup;
_sfc_main$8.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/TextareaField.vue");
  return _sfc_setup$8 ? _sfc_setup$8(props, ctx) : void 0;
};
const TextareaField = Object.assign(_sfc_main$8, { __name: "TextareaField" });
const _sfc_main$7 = /* @__PURE__ */ defineComponent({
  __name: "CheckboxList",
  __ssrInlineRender: true,
  props: {
    title: {},
    data: {},
    showCongViecKhac: { type: Boolean, default: false },
    congViecKhacChecked: { type: Boolean, default: false },
    congViecKhacText: { default: "" },
    isFormDisabled: { type: Boolean, default: false }
  },
  emits: ["toggle-item", "update-cong-viec-khac-tick", "update-cong-viec-khac-text"],
  setup(__props, { expose: __expose, emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const localCongViecKhacText = ref(props.congViecKhacText);
    watch(() => props.congViecKhacText, (newValue) => {
      localCongViecKhacText.value = newValue;
    });
    const handleTextareaBlur = () => {
      if (localCongViecKhacText.value !== props.congViecKhacText) {
        emit("update-cong-viec-khac-text", localCongViecKhacText.value);
      }
    };
    const GetCongViecKhac = () => {
      return {
        checked: props.congViecKhacChecked,
        text: props.congViecKhacText
      };
    };
    const UpdateCongViecKhac = (checked, text) => {
      emit("update-cong-viec-khac-tick", checked);
      emit("update-cong-viec-khac-text", text);
    };
    __expose({
      GetCongViecKhac,
      UpdateCongViecKhac
    });
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<div${ssrRenderAttrs(_attrs)} data-v-97a5e619><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm mt-4" data-v-97a5e619>${ssrInterpolate(__props.title)}</div>`);
      if (__props.data) {
        _push(`<div class="flex w-full" data-v-97a5e619><div class="flex flex-1 justify-start items-start" data-v-97a5e619><table class="w-full border-collapse" data-v-97a5e619><tbody data-v-97a5e619><!--[-->`);
        ssrRenderList(__props.data.Left, (item) => {
          _push(`<tr class="cursor-pointer hover:bg-blue-50 transition-colors duration-200 border-b border-gray-200 hover:border-blue-300 h-12 align-middle" data-v-97a5e619><td class="p-3 text-sm w-8 text-center align-middle" data-v-97a5e619><input type="checkbox"${ssrIncludeBooleanAttr(item.ND_ISTICK === "+") ? " checked" : ""} class="w-4 h-4 cursor-pointer accent-sky-700" data-v-97a5e619></td><td class="p-3 text-sm align-middle" data-v-97a5e619>${ssrInterpolate(item.ND_CONTENT)} / ${ssrInterpolate(item.ND_CONTENT2)}</td></tr>`);
        });
        _push(`<!--]--></tbody></table></div><div class="flex flex-1 justify-start items-start" data-v-97a5e619><table class="w-full border-collapse" data-v-97a5e619><tbody data-v-97a5e619><!--[-->`);
        ssrRenderList(__props.data.Right, (item) => {
          _push(`<tr class="cursor-pointer hover:bg-blue-50 transition-colors duration-200 border-b border-gray-200 hover:border-blue-300 h-12 align-middle" data-v-97a5e619><td class="p-3 text-sm w-8 text-center align-middle" data-v-97a5e619><input type="checkbox"${ssrIncludeBooleanAttr(item.ND_ISTICK === "+") ? " checked" : ""} class="w-4 h-4 cursor-pointer accent-sky-700" data-v-97a5e619></td><td class="p-3 text-sm align-middle" data-v-97a5e619>${ssrInterpolate(item.ND_CONTENT)} / ${ssrInterpolate(item.ND_CONTENT2)}</td></tr>`);
        });
        _push(`<!--]--></tbody></table></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (__props.showCongViecKhac) {
        _push(`<div class="w-full mt-4" data-v-97a5e619><div class="flex items-start" data-v-97a5e619><div class="p-3 text-sm w-8 text-center" data-v-97a5e619><input type="checkbox" id="congViecKhacCheckbox"${ssrIncludeBooleanAttr(__props.congViecKhacChecked) ? " checked" : ""}${ssrIncludeBooleanAttr(__props.isFormDisabled) ? " disabled" : ""} class="w-4 h-4 cursor-pointer accent-sky-700 disabled:cursor-not-allowed" data-v-97a5e619></div><div class="p-3 text-sm flex-1" data-v-97a5e619><div class="flex items-start gap-3" data-v-97a5e619><label for="congViecKhacCheckbox" class="text-sm whitespace-nowrap" data-v-97a5e619> Công việc khác <br data-v-97a5e619> Другое </label><div class="flex-1" data-v-97a5e619>`);
        _push(ssrRenderComponent(TextareaField, {
          label: "",
          modelValue: localCongViecKhacText.value,
          "onUpdate:modelValue": ($event) => localCongViecKhacText.value = $event,
          onBlur: handleTextareaBlur,
          placeholder: "Nhập ghi chú thêm về công việc khác...",
          rows: 4,
          width: "100%",
          height: "120px"
        }, null, _parent));
        _push(`</div></div></div></div></div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</div>`);
    };
  }
});
const _sfc_setup$7 = _sfc_main$7.setup;
_sfc_main$7.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/CheckboxList.vue");
  return _sfc_setup$7 ? _sfc_setup$7(props, ctx) : void 0;
};
const CheckboxList = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$7, [["__scopeId", "data-v-97a5e619"]]), { __name: "CheckboxList" });
const _sfc_main$6 = /* @__PURE__ */ defineComponent({
  __name: "TinhTrangThietBiSauBaoDuong",
  __ssrInlineRender: true,
  props: {
    wocode: {},
    initialTinhTrangThietBi: { default: null },
    initialTongThoiGian: { default: "" },
    initialNhanLuc: { default: "" },
    disabled: { type: Boolean, default: false }
  },
  emits: ["notification", "update:tinhTrangThietBi", "update:tongThoiGian", "update:nhanLuc"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const tinhTrangThietBi = ref(props.initialTinhTrangThietBi);
    const tongThoiGian = ref(props.initialTongThoiGian);
    const nhanLuc = ref(props.initialNhanLuc);
    watch(() => props.initialTinhTrangThietBi, (newVal) => {
      tinhTrangThietBi.value = newVal;
    });
    watch(() => props.initialTongThoiGian, (newVal) => {
      tongThoiGian.value = newVal;
    });
    watch(() => props.initialNhanLuc, (newVal) => {
      nhanLuc.value = newVal;
    });
    watch(tinhTrangThietBi, (newVal) => {
      emit("update:tinhTrangThietBi", newVal);
    });
    watch(tongThoiGian, (newVal) => {
      emit("update:tongThoiGian", newVal);
    });
    watch(nhanLuc, (newVal) => {
      emit("update:nhanLuc", newVal);
    });
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<div${ssrRenderAttrs(mergeProps({ class: "w-full mt-6" }, _attrs))} data-v-b153e6a7><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm" data-v-b153e6a7> 7. Tình trạng thiết bị sau bảo dưỡng / Состояние оборудования после ППР </div><div class="flex w-full mt-4" data-v-b153e6a7><div class="grid grid-cols-3 w-full gap-4 px-4" data-v-b153e6a7><div class="flex items-start" data-v-b153e6a7><input type="radio" id="tinhtrang1" name="tinhTrangThietBi"${ssrRenderAttr("value", 1)}${ssrIncludeBooleanAttr(tinhTrangThietBi.value === 1) ? " checked" : ""}${ssrIncludeBooleanAttr(__props.disabled) ? " disabled" : ""} class="w-4 h-4 mr-2 mt-1 accent-blue-600" data-v-b153e6a7><label for="tinhtrang1" class="text-sm cursor-pointer" data-v-b153e6a7> Tốt, sẵn sàng hoạt động<br data-v-b153e6a7><span class="text-xs text-gray-600" data-v-b153e6a7>Исправно, допускается к работе</span></label></div><div class="flex items-start" data-v-b153e6a7><input type="radio" id="tinhtrang2" name="tinhTrangThietBi"${ssrRenderAttr("value", 2)}${ssrIncludeBooleanAttr(tinhTrangThietBi.value === 2) ? " checked" : ""}${ssrIncludeBooleanAttr(__props.disabled) ? " disabled" : ""} class="w-4 h-4 mr-2 mt-1 accent-blue-600" data-v-b153e6a7><label for="tinhtrang2" class="text-sm cursor-pointer" data-v-b153e6a7> Có thể hoạt động tạm thời<br data-v-b153e6a7><span class="text-xs text-gray-600" data-v-b153e6a7>Исправно не в полн.объеме,допуск к раб.</span></label></div><div class="flex items-start" data-v-b153e6a7><input type="radio" id="tinhtrang3" name="tinhTrangThietBi"${ssrRenderAttr("value", 3)}${ssrIncludeBooleanAttr(tinhTrangThietBi.value === 3) ? " checked" : ""}${ssrIncludeBooleanAttr(__props.disabled) ? " disabled" : ""} class="w-4 h-4 mr-2 mt-1 accent-blue-600" data-v-b153e6a7><label for="tinhtrang3" class="text-sm cursor-pointer" data-v-b153e6a7> Cần sửa chữa, cấm hoạt động<br data-v-b153e6a7><span class="text-xs text-gray-600" data-v-b153e6a7>Треб. ремонт,<br data-v-b153e6a7>запрещено работать</span></label></div></div></div><div class="flex w-full mt-6 px-4" data-v-b153e6a7><div class="w-1/2 flex flex-col" data-v-b153e6a7><div class="flex items-center" data-v-b153e6a7><div class="text-sm font-medium mr-4 w-64 shrink-0" data-v-b153e6a7> Tổng thời gian thực hiện bảo dưỡng: </div><div class="relative flex items-center" data-v-b153e6a7><div class="w-64" data-v-b153e6a7><div class="w-64 rounded border border-slate-300 bg-slate-50 px-3 py-1 text-center text-sm text-slate-700" data-v-b153e6a7>${ssrInterpolate(tongThoiGian.value || "-")}</div></div><div class="w-10" data-v-b153e6a7><span class="ml-3 text-sm" data-v-b153e6a7>giờ</span></div></div></div><div class="flex items-center mt-2" data-v-b153e6a7><div class="text-xs text-gray-600 w-64 shrink-0" data-v-b153e6a7>TПрактич. время выполнения</div><div class="flex items-center" data-v-b153e6a7><div class="w-64" data-v-b153e6a7></div><div class="w-10" data-v-b153e6a7><span class="ml-7 text-xs" data-v-b153e6a7>часов</span></div></div></div></div><div class="w-1/2 flex flex-col" data-v-b153e6a7><div class="flex items-center" data-v-b153e6a7><div class="text-sm font-medium mr-4 w-56 shrink-0" data-v-b153e6a7> Nhân lực: </div><div class="relative flex items-center" data-v-b153e6a7><div class="w-64" data-v-b153e6a7><div class="w-64 rounded border border-slate-300 bg-slate-50 px-3 py-1 text-center text-sm text-slate-700" data-v-b153e6a7>${ssrInterpolate(nhanLuc.value || "-")}</div></div><div class="w-10" data-v-b153e6a7><span class="ml-3 text-sm" data-v-b153e6a7>người</span></div></div></div><div class="flex items-center mt-2" data-v-b153e6a7><div class="text-xs text-gray-600 w-56 shrink-0" data-v-b153e6a7>Колич. исполнителей</div><div class="flex items-center" data-v-b153e6a7><div class="w-64" data-v-b153e6a7></div><div class="w-10" data-v-b153e6a7><span class="ml-7 text-xs" data-v-b153e6a7>человек</span></div></div></div></div></div></div>`);
    };
  }
});
const _sfc_setup$6 = _sfc_main$6.setup;
_sfc_main$6.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/TinhTrangThietBiSauBaoDuong.vue");
  return _sfc_setup$6 ? _sfc_setup$6(props, ctx) : void 0;
};
const TinhTrangThietBiSauBaoDuong = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$6, [["__scopeId", "data-v-b153e6a7"]]), { __name: "TinhTrangThietBiSauBaoDuong" });
function toPartApiResult(response, defaultSuccessMessage, defaultFailMessage) {
  const root = response?.data || {};
  const payload = root?.data || root?.Data || {};
  const success = payload?.Success === true || payload?.IsSuccess === true || root?.Success === true || root?.IsSuccess === true || payload === true || root?.data === true;
  const message2 = payload?.Message || payload?.message || root?.message || root?.Message || (success ? defaultSuccessMessage : defaultFailMessage);
  return { success, message: message2 };
}
function extractPartApiErrorMessage(error, fallback) {
  const responseData = error?.response?.data || {};
  const payload = responseData?.data || responseData?.Data || {};
  return payload?.Message || payload?.message || responseData?.message || responseData?.Message || fallback;
}
async function getPartsByWO(org, wo) {
  try {
    if (!wo) return [];
    const { $axios } = useNuxtApp();
    const response = await $axios.get("/Part/GetAll", {
      params: { org: org || "", wo }
    });
    const data = response.data?.data;
    const items = Array.isArray(data) ? data.map((d) => ({
      id: String(d.id ?? ""),
      partcode: String(d.partcode ?? ""),
      partdescription: String(d.partdescription ?? ""),
      storecode: d.storecode ?? "",
      partuom: String(d.partuom ?? ""),
      usedqty: String(d.usedqty ?? d.plannedqty ?? ""),
      workordernum: String(d.workordernum ?? ""),
      matlist_lineno: String(d.matlist_lineno ?? ""),
      partorganization: String(d.partorganization ?? (org || "")),
      woattachto: d.woattachto ?? ""
    })) : [];
    return items;
  } catch (error) {
    console.error("Error getPartsByWO:", error);
    throw error;
  }
}
async function createPartWO(params) {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.post("/Part/Create", null, {
      params: {
        workordernum: params.workordernum,
        org: params.org,
        qty: String(params.qty),
        woequipment: params.woequipment,
        partcode: params.partcode,
        partdescription: params.partdescription,
        partuom: params.partuom
      }
    });
    return toPartApiResult(response, "Thêm vật tư thành công!", "Không thể thêm vật tư!");
  } catch (error) {
    console.error("Error createPartWO:", error);
    return {
      success: false,
      message: extractPartApiErrorMessage(error, "Lỗi khi gọi API tạo vật tư!")
    };
  }
}
async function updatePartIssue(params) {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.put("/Part/UpdateIssue", null, {
      params: {
        workordernum: params.workordernum,
        org: params.org,
        qty: String(params.qty),
        woequipment: params.woequipment,
        partcode: params.partcode
      }
    });
    return toPartApiResult(response, "Cập nhật vật tư thành công!", "Không thể cập nhật vật tư!");
  } catch (error) {
    console.error("Error updatePartIssue:", error);
    return {
      success: false,
      message: extractPartApiErrorMessage(error, "Lỗi khi gọi API cấp thêm vật tư!")
    };
  }
}
async function updatePartReturn(params) {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.put("/Part/UpdateReturn", null, {
      params: {
        workordernum: params.workordernum,
        org: params.org,
        qty: String(params.qty),
        woequipment: params.woequipment,
        partcode: params.partcode
      }
    });
    return toPartApiResult(response, "Hoàn trả vật tư thành công!", "Không thể hoàn trả vật tư!");
  } catch (error) {
    console.error("Error updatePartReturn:", error);
    return {
      success: false,
      message: extractPartApiErrorMessage(error, "Lỗi khi gọi API hoàn trả vật tư!")
    };
  }
}
const GetAllPart = async (page = 1, pageSize2 = 10, search, gian) => {
  try {
    const { $axios } = useNuxtApp();
    const params = {
      page,
      pageSize: pageSize2
    };
    if (search) {
      params.search = search;
    }
    if (gian) {
      params.gian = gian;
    }
    const response = await $axios.get("/VTTH/GetAllPUPPart", {
      params
    });
    return response.data;
  } catch (error) {
    console.error("Error fetching VatTu parts:", error);
    throw error;
  }
};
const _sfc_main$5 = /* @__PURE__ */ defineComponent({
  __name: "VatTuSelectionModal",
  __ssrInlineRender: true,
  props: {
    visible: { type: Boolean },
    gian: {}
  },
  emits: ["update:visible", "vattu-selected"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const visible = ref(props.visible);
    const loading = ref(false);
    const searchText = ref("");
    const currentPage = ref(1);
    const pageSize2 = ref(10);
    const totalItems = ref(0);
    const vatTuData = ref([]);
    const columns = [
      {
        title: "Mã vật tư",
        dataIndex: "par_code",
        key: "par_code",
        width: 150
      },
      {
        title: "Tên vật tư",
        dataIndex: "par_desc",
        key: "par_desc",
        width: 300,
        align: "left"
      },
      {
        title: "Mã kho",
        dataIndex: "sto_store",
        key: "sto_store",
        width: 120
      },
      {
        title: "Số lượng tồn",
        dataIndex: "STO_QTY",
        key: "STO_QTY",
        width: 120,
        align: "center"
      },
      {
        title: "Đơn vị tính",
        dataIndex: "PAR_UOM",
        key: "PAR_UOM",
        width: 100,
        align: "center"
      },
      {
        title: "Vị trí",
        dataIndex: "BIS_BIN",
        key: "BIS_BIN",
        width: 120
      }
    ];
    watch(() => props.visible, (newVal) => {
      visible.value = newVal;
      if (newVal) {
        fetchVatTu();
      }
    });
    watch(visible, (newVal) => {
      emit("update:visible", newVal);
    });
    const fetchVatTu = async () => {
      try {
        loading.value = true;
        const response = await GetAllPart(
          currentPage.value,
          pageSize2.value,
          searchText.value || void 0,
          props.gian
        );
        if (response?.IsSuccess) {
          vatTuData.value = response.Data || [];
          const firstItem = vatTuData.value?.[0];
          const serverTotalRows = firstItem?.total_rows;
          const serverTotalPages = firstItem?.total_pages;
          if (typeof serverTotalRows === "number" && serverTotalRows > 0) {
            totalItems.value = serverTotalRows;
          } else if (typeof serverTotalPages === "number" && serverTotalPages > 0) {
            totalItems.value = serverTotalPages * pageSize2.value;
          } else {
            totalItems.value = vatTuData.value?.length || 0;
          }
        } else {
          vatTuData.value = [];
          totalItems.value = 0;
        }
      } catch (error) {
        console.error("Error fetching vat tu:", error);
        vatTuData.value = [];
        totalItems.value = 0;
      } finally {
        loading.value = false;
      }
    };
    const handleCancel = () => {
      visible.value = false;
      resetSelection();
    };
    const handleSearch = debounce(() => {
      currentPage.value = 1;
      fetchVatTu();
    }, 500);
    const handlePageChange = (page) => {
      currentPage.value = page;
      fetchVatTu();
    };
    const handlePageSizeChange = (current, size) => {
      currentPage.value = 1;
      pageSize2.value = size;
      fetchVatTu();
    };
    const handleRowClick = async (selectedVatTu) => {
      try {
        emit("vattu-selected", selectedVatTu);
        visible.value = false;
        resetSelection();
      } catch (error) {
        console.error("Error selecting vat tu:", error);
      }
    };
    const resetSelection = () => {
      searchText.value = "";
      currentPage.value = 1;
    };
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_modal = resolveComponent("a-modal");
      const _component_a_input = resolveComponent("a-input");
      const _component_a_table = resolveComponent("a-table");
      _push(ssrRenderComponent(_component_a_modal, mergeProps({
        open: visible.value,
        "onUpdate:open": ($event) => visible.value = $event,
        title: "Chọn vật tư tiêu hao",
        width: "1200px",
        footer: null,
        onCancel: handleCancel,
        "body-style": { maxHeight: "70vh", overflow: "hidden" }
      }, _attrs), {
        default: withCtx((_, _push2, _parent2, _scopeId) => {
          if (_push2) {
            _push2(`<div class="vattu-selection-modal" data-v-79f2e967${_scopeId}><div class="mb-4 flex justify-end" data-v-79f2e967${_scopeId}>`);
            _push2(ssrRenderComponent(_component_a_input, {
              value: searchText.value,
              "onUpdate:value": ($event) => searchText.value = $event,
              placeholder: "Tìm kiếm vật tư...",
              "allow-clear": "",
              onInput: unref(handleSearch),
              style: { "width": "30%" }
            }, {
              suffix: withCtx((_2, _push3, _parent3, _scopeId2) => {
                if (_push3) {
                  _push3(ssrRenderComponent(unref(SearchOutlined), null, null, _parent3, _scopeId2));
                } else {
                  return [
                    createVNode(unref(SearchOutlined))
                  ];
                }
              }),
              _: 1
            }, _parent2, _scopeId));
            _push2(`</div>`);
            _push2(ssrRenderComponent(_component_a_table, {
              columns,
              "data-source": vatTuData.value,
              loading: loading.value,
              pagination: {
                current: currentPage.value,
                pageSize: pageSize2.value,
                total: totalItems.value,
                showSizeChanger: true,
                showQuickJumper: true,
                showTotal: (total, range) => `${range[0]}-${range[1]} của ${total} vật tư`,
                onChange: handlePageChange,
                onShowSizeChange: handlePageSizeChange
              },
              size: "small",
              class: "vattu-table",
              scroll: { y: 400 },
              customRow: (record) => ({
                onClick: () => handleRowClick(record)
              })
            }, null, _parent2, _scopeId));
            _push2(`</div>`);
          } else {
            return [
              createVNode("div", { class: "vattu-selection-modal" }, [
                createVNode("div", { class: "mb-4 flex justify-end" }, [
                  createVNode(_component_a_input, {
                    value: searchText.value,
                    "onUpdate:value": ($event) => searchText.value = $event,
                    placeholder: "Tìm kiếm vật tư...",
                    "allow-clear": "",
                    onInput: unref(handleSearch),
                    style: { "width": "30%" }
                  }, {
                    suffix: withCtx(() => [
                      createVNode(unref(SearchOutlined))
                    ]),
                    _: 1
                  }, 8, ["value", "onUpdate:value", "onInput"])
                ]),
                createVNode(_component_a_table, {
                  columns,
                  "data-source": vatTuData.value,
                  loading: loading.value,
                  pagination: {
                    current: currentPage.value,
                    pageSize: pageSize2.value,
                    total: totalItems.value,
                    showSizeChanger: true,
                    showQuickJumper: true,
                    showTotal: (total, range) => `${range[0]}-${range[1]} của ${total} vật tư`,
                    onChange: handlePageChange,
                    onShowSizeChange: handlePageSizeChange
                  },
                  size: "small",
                  class: "vattu-table",
                  scroll: { y: 400 },
                  customRow: (record) => ({
                    onClick: () => handleRowClick(record)
                  })
                }, null, 8, ["data-source", "loading", "pagination", "customRow"])
              ])
            ];
          }
        }),
        _: 1
      }, _parent));
    };
  }
});
const _sfc_setup$5 = _sfc_main$5.setup;
_sfc_main$5.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/VatTuSelectionModal.vue");
  return _sfc_setup$5 ? _sfc_setup$5(props, ctx) : void 0;
};
const VatTuSelectionModal = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$5, [["__scopeId", "data-v-79f2e967"]]), { __name: "VatTuSelectionModal" });
const _sfc_main$4 = /* @__PURE__ */ defineComponent({
  __name: "VatTuTieuHaoWO",
  __ssrInlineRender: true,
  props: {
    org: { default: void 0 },
    wo: {},
    disabled: { type: Boolean, default: false },
    woequipment: {},
    gian: {}
  },
  setup(__props) {
    const props = __props;
    const tableData = ref([]);
    const loading = ref(false);
    const showNotification = ref(false);
    const notificationMessage = ref("Tải dữ liệu vật tư thành công!");
    const showVatTuModal = ref(false);
    const editingQtyId = ref(null);
    const editingQtyValue = ref(null);
    const qtyInputRef = ref(null);
    const qtyEditWrapRef = ref(null);
    const isSavingQty = ref(false);
    const displayData = computed(() => {
      const visibleRows = tableData.value.filter((item) => {
        const qty = normalizeQty(item.usedqty);
        if (qty === 0) return false;
        return true;
      });
      const searchRow = {
        id: "",
        partcode: "",
        partdescription: "",
        storecode: "",
        partuom: "",
        usedqty: "",
        __searchRow: true
      };
      return [...visibleRows, searchRow];
    });
    const columns = [
      { title: "STT", key: "stt", width: 80, align: "center" },
      { title: "Mã Vật Tư", dataIndex: "partcode", key: "partcode", width: 160, align: "center" },
      { title: "Tên Vật Tư", dataIndex: "partdescription", key: "partdescription", width: 300, align: "center" },
      { title: "Mã kho", dataIndex: "storecode", key: "storecode", width: 140, align: "center" },
      { title: "Đơn vị tính", dataIndex: "partuom", key: "partuom", width: 100, align: "center" },
      { title: "Số lượng", dataIndex: "usedqty", key: "usedqty", width: 200, align: "center" }
    ];
    const resolveWoForRequest = () => {
      return String(props.wo || "").trim();
    };
    const normalizeQty = (value) => {
      if (value === null || value === void 0 || value === "") return null;
      const parsed = Number(value);
      if (!Number.isFinite(parsed) || parsed < 0) return null;
      if (!Number.isInteger(parsed)) return null;
      return parsed;
    };
    const isPersistedRow = (record) => {
      return Boolean(record.matlist_lineno);
    };
    const fetchParts = async () => {
      const woForRequest = resolveWoForRequest();
      if (!woForRequest) return;
      try {
        loading.value = true;
        const items = await getPartsByWO(props.org || "", woForRequest);
        tableData.value = items;
      } catch (error) {
        console.error("Error fetching parts:", error);
      } finally {
        loading.value = false;
      }
    };
    const openVatTuModal = () => {
      showVatTuModal.value = true;
    };
    const gianForVatTu = computed(() => props.gian || props.org || "");
    const handleVatTuSelected = (vatTu) => {
      const woForRequest = resolveWoForRequest();
      const newId = String(Date.now());
      const newItem = {
        id: newId,
        partcode: vatTu.par_code,
        partdescription: vatTu.par_desc,
        storecode: vatTu.sto_store,
        partuom: vatTu.PAR_UOM,
        usedqty: "",
        workordernum: woForRequest,
        partorganization: props.org || "",
        woattachto: props.woequipment || null
      };
      tableData.value = [...tableData.value, newItem];
      editingQtyId.value = newId;
      editingQtyValue.value = null;
      showVatTuModal.value = false;
      nextTick(() => {
        qtyInputRef.value?.focus?.();
      });
    };
    const handleNotificationHide = () => {
      showNotification.value = false;
    };
    watch(() => props.wo, () => {
      fetchParts();
    });
    const cancelEditQty = (record, force = false) => {
      if (isSavingQty.value && !force) return;
      if (record && !record.matlist_lineno) {
        tableData.value = tableData.value.filter((i) => i.id !== record.id);
      }
      editingQtyId.value = null;
      editingQtyValue.value = null;
    };
    const startEditQty = (record) => {
      if (props.disabled) return;
      if (isSavingQty.value) return;
      editingQtyId.value = record.id;
      editingQtyValue.value = normalizeQty(record.usedqty) ?? 0;
      nextTick(() => {
        qtyInputRef.value?.focus?.();
      });
    };
    const handleQtySubmit = async (record) => {
      if (!editingQtyId.value || editingQtyId.value !== record.id) return;
      if (isSavingQty.value) return;
      try {
        isSavingQty.value = true;
        const qtyNumber = normalizeQty(editingQtyValue.value);
        if (qtyNumber === null) {
          notificationMessage.value = "Số lượng phải là số nguyên >= 0!";
          showNotification.value = true;
          nextTick(() => {
            qtyInputRef.value?.focus?.();
          });
          return;
        }
        const originalQtyNumber = normalizeQty(record.usedqty) ?? 0;
        if (record.matlist_lineno && qtyNumber === originalQtyNumber) {
          cancelEditQty();
          return;
        }
        const woForRequest = resolveWoForRequest() || record.workordernum || "";
        if (!woForRequest) {
          notificationMessage.value = "Thiếu mã WO để thao tác vật tư!";
          showNotification.value = true;
          return;
        }
        let success = false;
        let message2 = "";
        if (record.matlist_lineno) {
          const qtyDiff = qtyNumber - originalQtyNumber;
          const updateResult = qtyDiff > 0 ? await updatePartIssue({
            workordernum: woForRequest,
            org: props.org || "",
            qty: qtyDiff,
            woequipment: props.woequipment || record.woattachto || "",
            partcode: record.partcode
          }) : await updatePartReturn({
            workordernum: woForRequest,
            org: props.org || "",
            qty: Math.abs(qtyDiff),
            woequipment: props.woequipment || record.woattachto || "",
            partcode: record.partcode
          });
          success = updateResult.success;
          message2 = updateResult.message;
          if (success) {
            record.usedqty = String(qtyNumber);
            if (qtyNumber === 0) {
              tableData.value = tableData.value.filter((i) => i.id !== record.id);
            }
            cancelEditQty(void 0, true);
          }
        } else {
          if (qtyNumber <= 0) {
            notificationMessage.value = "Vật tư mới phải nhập số lượng lớn hơn 0!";
            showNotification.value = true;
            nextTick(() => {
              qtyInputRef.value?.focus?.();
            });
            return;
          }
          const createResult = await createPartWO({
            workordernum: woForRequest,
            org: props.org || "",
            qty: qtyNumber,
            woequipment: props.woequipment || record.woattachto || "",
            partcode: record.partcode,
            partdescription: record.partdescription,
            partuom: record.partuom
          });
          success = createResult.success;
          message2 = createResult.message;
          if (success) {
            record.usedqty = String(qtyNumber);
            if (qtyNumber === 0) {
              tableData.value = tableData.value.filter((i) => i.id !== record.id);
            }
            record.matlist_lineno = record.matlist_lineno || "LOCAL_CREATED";
            cancelEditQty(void 0, true);
          }
        }
        notificationMessage.value = message2 || (success ? "Thao tác thành công!" : "Thao tác thất bại!");
        showNotification.value = true;
      } catch (error) {
        console.error("Error create/update part:", error);
        notificationMessage.value = "Lỗi khi gọi API vật tư!";
        showNotification.value = true;
      } finally {
        isSavingQty.value = false;
      }
    };
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_table = resolveComponent("a-table");
      const _component_a_input_number = resolveComponent("a-input-number");
      const _component_a_button = resolveComponent("a-button");
      const _component_NotificationToast = __nuxt_component_2;
      _push(`<!--[--><div class="w-full mt-6" data-v-eaff366b><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm" data-v-eaff366b> 8. Vật tư tiêu hao / Израсходованные материалы </div><div class="mt-4 overflow-x-auto" data-v-eaff366b>`);
      _push(ssrRenderComponent(_component_a_table, {
        "data-source": displayData.value,
        columns,
        bordered: "",
        loading: loading.value,
        pagination: false,
        "table-layout": "fixed",
        size: "small",
        class: "vat-tu-tieu-hao-wo-table",
        scroll: { x: 980 }
      }, {
        bodyCell: withCtx(({ column, record, index, text }, _push2, _parent2, _scopeId) => {
          if (_push2) {
            if (column.key === "stt") {
              _push2(`<div class="text-center" data-v-eaff366b${_scopeId}>${ssrInterpolate(record.__searchRow ? "" : index + 1)}</div>`);
            } else {
              _push2(`<!--[-->`);
              if (record.__searchRow) {
                _push2(`<!--[-->`);
                if (column.key === "partcode") {
                  _push2(`<div class="text-gray-400 italic cursor-pointer" data-v-eaff366b${_scopeId}>`);
                  _push2(ssrRenderComponent(unref(SearchOutlined), null, null, _parent2, _scopeId));
                  _push2(`<span class="ml-1" data-v-eaff366b${_scopeId}>Choose parts</span></div>`);
                } else {
                  _push2(`<span data-v-eaff366b${_scopeId}></span>`);
                }
                _push2(`<!--]-->`);
              } else {
                _push2(`<!--[-->`);
                if (column.key === "usedqty") {
                  _push2(`<!--[-->`);
                  if (editingQtyId.value === record.id) {
                    _push2(`<div class="${ssrRenderClass([{ "qty-edit-wrap--with-cancel": !isPersistedRow(record) }, "qty-edit-wrap"])}" data-v-eaff366b${_scopeId}>`);
                    _push2(ssrRenderComponent(_component_a_input_number, {
                      size: "small",
                      value: editingQtyValue.value,
                      "onUpdate:value": ($event) => editingQtyValue.value = $event,
                      min: 0,
                      precision: 0,
                      step: 1,
                      disabled: isSavingQty.value,
                      onPressEnter: ($event) => handleQtySubmit(record),
                      onKeydown: ($event) => cancelEditQty(record),
                      ref_key: "qtyInputRef",
                      ref: qtyInputRef,
                      class: "qty-input"
                    }, null, _parent2, _scopeId));
                    _push2(`<div class="qty-actions" data-v-eaff366b${_scopeId}>`);
                    _push2(ssrRenderComponent(_component_a_button, {
                      size: "small",
                      type: "primary",
                      loading: isSavingQty.value,
                      disabled: isSavingQty.value,
                      onClick: ($event) => handleQtySubmit(record)
                    }, {
                      default: withCtx((_, _push3, _parent3, _scopeId2) => {
                        if (_push3) {
                          _push3(` Save `);
                        } else {
                          return [
                            createTextVNode(" Save ")
                          ];
                        }
                      }),
                      _: 2
                    }, _parent2, _scopeId));
                    if (!isPersistedRow(record)) {
                      _push2(ssrRenderComponent(_component_a_button, {
                        size: "small",
                        disabled: isSavingQty.value,
                        onClick: ($event) => cancelEditQty(record)
                      }, {
                        default: withCtx((_, _push3, _parent3, _scopeId2) => {
                          if (_push3) {
                            _push3(` Cancel `);
                          } else {
                            return [
                              createTextVNode(" Cancel ")
                            ];
                          }
                        }),
                        _: 2
                      }, _parent2, _scopeId));
                    } else {
                      _push2(`<!---->`);
                    }
                    _push2(`</div></div>`);
                  } else {
                    _push2(ssrRenderComponent(_component_a_button, {
                      type: "link",
                      class: "qty-display-btn",
                      disabled: __props.disabled,
                      onClick: ($event) => startEditQty(record)
                    }, {
                      default: withCtx((_, _push3, _parent3, _scopeId2) => {
                        if (_push3) {
                          _push3(`${ssrInterpolate(record.usedqty || "0")}`);
                        } else {
                          return [
                            createTextVNode(toDisplayString(record.usedqty || "0"), 1)
                          ];
                        }
                      }),
                      _: 2
                    }, _parent2, _scopeId));
                  }
                  _push2(`<!--]-->`);
                } else {
                  _push2(`<span class="text-gray-900" data-v-eaff366b${_scopeId}>${ssrInterpolate(text || "")}</span>`);
                }
                _push2(`<!--]-->`);
              }
              _push2(`<!--]-->`);
            }
          } else {
            return [
              column.key === "stt" ? (openBlock(), createBlock("div", {
                key: 0,
                class: "text-center"
              }, toDisplayString(record.__searchRow ? "" : index + 1), 1)) : (openBlock(), createBlock(Fragment, { key: 1 }, [
                record.__searchRow ? (openBlock(), createBlock(Fragment, { key: 0 }, [
                  column.key === "partcode" ? (openBlock(), createBlock("div", {
                    key: 0,
                    class: "text-gray-400 italic cursor-pointer",
                    onClick: openVatTuModal
                  }, [
                    createVNode(unref(SearchOutlined)),
                    createVNode("span", { class: "ml-1" }, "Choose parts")
                  ])) : (openBlock(), createBlock("span", { key: 1 }))
                ], 64)) : (openBlock(), createBlock(Fragment, { key: 1 }, [
                  column.key === "usedqty" ? (openBlock(), createBlock(Fragment, { key: 0 }, [
                    editingQtyId.value === record.id ? (openBlock(), createBlock("div", {
                      key: 0,
                      ref_key: "qtyEditWrapRef",
                      ref: qtyEditWrapRef,
                      class: ["qty-edit-wrap", { "qty-edit-wrap--with-cancel": !isPersistedRow(record) }]
                    }, [
                      createVNode(_component_a_input_number, {
                        size: "small",
                        value: editingQtyValue.value,
                        "onUpdate:value": ($event) => editingQtyValue.value = $event,
                        min: 0,
                        precision: 0,
                        step: 1,
                        disabled: isSavingQty.value,
                        onPressEnter: ($event) => handleQtySubmit(record),
                        onKeydown: withKeys(($event) => cancelEditQty(record), ["esc"]),
                        ref_key: "qtyInputRef",
                        ref: qtyInputRef,
                        class: "qty-input"
                      }, null, 8, ["value", "onUpdate:value", "disabled", "onPressEnter", "onKeydown"]),
                      createVNode("div", { class: "qty-actions" }, [
                        createVNode(_component_a_button, {
                          size: "small",
                          type: "primary",
                          loading: isSavingQty.value,
                          disabled: isSavingQty.value,
                          onClick: ($event) => handleQtySubmit(record)
                        }, {
                          default: withCtx(() => [
                            createTextVNode(" Save ")
                          ]),
                          _: 1
                        }, 8, ["loading", "disabled", "onClick"]),
                        !isPersistedRow(record) ? (openBlock(), createBlock(_component_a_button, {
                          key: 0,
                          size: "small",
                          disabled: isSavingQty.value,
                          onClick: ($event) => cancelEditQty(record)
                        }, {
                          default: withCtx(() => [
                            createTextVNode(" Cancel ")
                          ]),
                          _: 1
                        }, 8, ["disabled", "onClick"])) : createCommentVNode("", true)
                      ])
                    ], 2)) : (openBlock(), createBlock(_component_a_button, {
                      key: 1,
                      type: "link",
                      class: "qty-display-btn",
                      disabled: __props.disabled,
                      onClick: ($event) => startEditQty(record)
                    }, {
                      default: withCtx(() => [
                        createTextVNode(toDisplayString(record.usedqty || "0"), 1)
                      ]),
                      _: 2
                    }, 1032, ["disabled", "onClick"]))
                  ], 64)) : (openBlock(), createBlock("span", {
                    key: 1,
                    class: "text-gray-900"
                  }, toDisplayString(text || ""), 1))
                ], 64))
              ], 64))
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div></div>`);
      _push(ssrRenderComponent(_component_NotificationToast, {
        show: showNotification.value,
        message: notificationMessage.value,
        duration: 2e3,
        onHide: handleNotificationHide
      }, null, _parent));
      _push(ssrRenderComponent(VatTuSelectionModal, {
        visible: showVatTuModal.value,
        "onUpdate:visible": ($event) => showVatTuModal.value = $event,
        gian: gianForVatTu.value,
        onVattuSelected: handleVatTuSelected
      }, null, _parent));
      _push(`<!--]-->`);
    };
  }
});
const _sfc_setup$4 = _sfc_main$4.setup;
_sfc_main$4.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/VatTuTieuHaoWO.vue");
  return _sfc_setup$4 ? _sfc_setup$4(props, ctx) : void 0;
};
const VatTuTieuHaoWO = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$4, [["__scopeId", "data-v-eaff366b"]]), { __name: "VatTuTieuHaoWO" });
const _sfc_main$3 = /* @__PURE__ */ defineComponent({
  __name: "ToolEquipmentTable",
  __ssrInlineRender: true,
  props: {
    toolData: {},
    toolLoading: { type: Boolean }
  },
  emits: ["openToolModal", "deleteToolItem"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const toolColumns = [
      {
        title: "STT",
        key: "stt",
        width: 60,
        align: "center"
      },
      {
        title: () => h("div", { innerHTML: "Tên thiết bị<br/>Наименование" }),
        dataIndex: "Ten_TB",
        key: "Ten_TB",
        width: 200
      },
      {
        title: () => h("div", { innerHTML: "Ký mã hiệu<br/>Тип, модель" }),
        dataIndex: "Ma_Ky_Hieu",
        key: "Ma_Ky_Hieu",
        width: 120
      },
      {
        title: () => h("div", { innerHTML: "Số seri<br/>Заводской номер" }),
        dataIndex: "So_Seri",
        key: "So_Seri",
        width: 120
      },
      {
        title: () => h("div", { innerHTML: "Nhà sản xuất<br/>Производитель" }),
        dataIndex: "Nha_SX",
        key: "Nha_SX",
        width: 120
      },
      {
        title: () => h("div", { innerHTML: "Hạng kiểm định<br/>Дата поверки" }),
        dataIndex: "Hang_Kiem_Dinh",
        key: "Hang_Kiem_Dinh",
        width: 150,
        customRender: ({ text }) => {
          if (text) {
            return new Date(text).toLocaleDateString("vi-VN");
          }
          return "";
        }
      },
      {
        title: "",
        key: "action",
        width: 80,
        align: "center"
      }
    ];
    const toolDataWithEmptyRow = computed(() => {
      const emptyRow = {
        Ma_Tool: "",
        Ten_TB: "",
        Ma_Ky_Hieu: "",
        So_Seri: "",
        Nha_SX: "",
        Hang_Kiem_Dinh: ""
      };
      return [...props.toolData, emptyRow];
    });
    const openToolModal = () => {
      emit("openToolModal");
    };
    const handleDeleteTool = (tool) => {
      emit("deleteToolItem", tool);
    };
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_table = resolveComponent("a-table");
      const _component_a_button = resolveComponent("a-button");
      _push(`<div${ssrRenderAttrs(_attrs)} data-v-3108f72c><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm mt-4" data-v-3108f72c> 3. Danh mục thiết bị chuẩn sử dụng cho việc bảo dưỡng / Используемое оборудование и приборы </div><div class="mt-2" data-v-3108f72c>`);
      _push(ssrRenderComponent(_component_a_table, {
        columns: toolColumns,
        "data-source": toolDataWithEmptyRow.value,
        loading: __props.toolLoading,
        pagination: false,
        size: "small",
        class: "tool-table thin-border",
        "custom-row": (record, index) => {
          return {
            onClick: () => {
              if (typeof index !== "undefined" && index >= __props.toolData.length) {
                openToolModal();
              }
            }
          };
        }
      }, {
        bodyCell: withCtx(({ column, text, record, index }, _push2, _parent2, _scopeId) => {
          if (_push2) {
            if (column.key === "stt") {
              _push2(`<!--[-->${ssrInterpolate(index < __props.toolData.length ? index + 1 : "")}<!--]-->`);
            } else {
              _push2(`<!---->`);
            }
            if (column.key === "Ten_TB") {
              _push2(`<!--[-->`);
              if (index >= __props.toolData.length) {
                _push2(`<div class="text-gray-400 italic cursor-pointer text-center" data-v-3108f72c${_scopeId}>`);
                _push2(ssrRenderComponent(unref(SearchOutlined), null, null, _parent2, _scopeId));
                _push2(`<span class="ml-1" data-v-3108f72c${_scopeId}>Choose tools</span></div>`);
              } else {
                _push2(`<!--[-->${ssrInterpolate(text)}<!--]-->`);
              }
              _push2(`<!--]-->`);
            } else {
              _push2(`<!---->`);
            }
            if (column.key === "action") {
              _push2(`<!--[-->`);
              if (index < __props.toolData.length) {
                _push2(ssrRenderComponent(_component_a_button, {
                  type: "primary",
                  size: "small",
                  class: "action-delete-btn",
                  onClick: ($event) => handleDeleteTool(record)
                }, {
                  icon: withCtx((_, _push3, _parent3, _scopeId2) => {
                    if (_push3) {
                      _push3(ssrRenderComponent(unref(DeleteOutlined), null, null, _parent3, _scopeId2));
                    } else {
                      return [
                        createVNode(unref(DeleteOutlined))
                      ];
                    }
                  }),
                  _: 2
                }, _parent2, _scopeId));
              } else {
                _push2(`<!---->`);
              }
              _push2(`<!--]-->`);
            } else {
              _push2(`<!---->`);
            }
          } else {
            return [
              column.key === "stt" ? (openBlock(), createBlock(Fragment, { key: 0 }, [
                createTextVNode(toDisplayString(index < __props.toolData.length ? index + 1 : ""), 1)
              ], 64)) : createCommentVNode("", true),
              column.key === "Ten_TB" ? (openBlock(), createBlock(Fragment, { key: 1 }, [
                index >= __props.toolData.length ? (openBlock(), createBlock("div", {
                  key: 0,
                  class: "text-gray-400 italic cursor-pointer text-center",
                  onClick: withModifiers(($event) => openToolModal(), ["stop"])
                }, [
                  createVNode(unref(SearchOutlined)),
                  createVNode("span", { class: "ml-1" }, "Choose tools")
                ], 8, ["onClick"])) : (openBlock(), createBlock(Fragment, { key: 1 }, [
                  createTextVNode(toDisplayString(text), 1)
                ], 64))
              ], 64)) : createCommentVNode("", true),
              column.key === "action" ? (openBlock(), createBlock(Fragment, { key: 2 }, [
                index < __props.toolData.length ? (openBlock(), createBlock(_component_a_button, {
                  key: 0,
                  type: "primary",
                  size: "small",
                  class: "action-delete-btn",
                  onClick: withModifiers(($event) => handleDeleteTool(record), ["stop"])
                }, {
                  icon: withCtx(() => [
                    createVNode(unref(DeleteOutlined))
                  ]),
                  _: 1
                }, 8, ["onClick"])) : createCommentVNode("", true)
              ], 64)) : createCommentVNode("", true)
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div></div>`);
    };
  }
});
const _sfc_setup$3 = _sfc_main$3.setup;
_sfc_main$3.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/ToolEquipmentTable.vue");
  return _sfc_setup$3 ? _sfc_setup$3(props, ctx) : void 0;
};
const ToolEquipmentTable = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$3, [["__scopeId", "data-v-3108f72c"]]), { __name: "ToolEquipmentTable" });
const _sfc_main$2 = /* @__PURE__ */ defineComponent({
  __name: "ToolSelectionModal",
  __ssrInlineRender: true,
  props: {
    visible: { type: Boolean },
    gian: {},
    bophan: {}
  },
  emits: ["update:visible", "tool-added"],
  setup(__props, { emit: __emit }) {
    const route = useRoute();
    const wocode = route.query.wocode;
    const props = __props;
    const emit = __emit;
    const visible = ref(props.visible);
    const loading = ref(false);
    const searchText = ref("");
    const currentPage = ref(1);
    const pageSize2 = ref(10);
    const totalItems = ref(0);
    const toolsData = ref([]);
    const columns = [
      {
        title: "Mã thiết bị",
        dataIndex: "Ma_Tool",
        key: "Ma_Tool",
        width: 150,
        align: "left"
      },
      {
        title: "Tên thiết bị",
        dataIndex: "Ten_TB",
        key: "Ten_TB",
        width: 200,
        align: "left"
      },
      {
        title: "Nhà sản xuất",
        dataIndex: "Nha_SX",
        key: "Nha_SX",
        width: 120
      },
      {
        title: "Số seri",
        dataIndex: "So_Seri",
        key: "So_Seri",
        width: 120
      },
      {
        title: "Mã ký hiệu",
        dataIndex: "Ma_Ky_Hieu",
        key: "Ma_Ky_Hieu",
        width: 120
      },
      {
        title: "Hạng kiểm định",
        dataIndex: "Hang_Kiem_Dinh",
        key: "Hang_Kiem_Dinh",
        width: 120,
        customRender: ({ text }) => {
          if (text) {
            const date = new Date(text);
            return date.toLocaleDateString("vi-VN");
          }
          return "";
        }
      }
    ];
    watch(() => props.visible, (newVal) => {
      visible.value = newVal;
      if (newVal) {
        fetchTools();
      }
    });
    watch(visible, (newVal) => {
      emit("update:visible", newVal);
    });
    const fetchTools = async () => {
      try {
        loading.value = true;
        const response = await GetToolsPaged({
          currentPage: currentPage.value,
          pageSize: pageSize2.value,
          gian: props.gian || "",
          bophan: props.bophan || "",
          search: searchText.value || void 0
        });
        if (response?.IsSuccess) {
          toolsData.value = response.Data || [];
          const firstItem = toolsData.value?.[0];
          const serverTotalRows = firstItem?.total_rows;
          const serverTotalPages = firstItem?.total_pages;
          if (typeof response.TotalCount === "number" && response.TotalCount > 0) {
            totalItems.value = response.TotalCount;
          } else if (typeof serverTotalRows === "number" && serverTotalRows > 0) {
            totalItems.value = serverTotalRows;
          } else if (typeof serverTotalPages === "number" && serverTotalPages > 0) {
            totalItems.value = serverTotalPages * pageSize2.value;
          } else {
            totalItems.value = toolsData.value?.length || 0;
          }
        } else {
          toolsData.value = [];
          totalItems.value = 0;
        }
      } catch (error) {
        console.error("Error fetching tools:", error);
        toolsData.value = [];
        totalItems.value = 0;
      } finally {
        loading.value = false;
      }
    };
    const handleCancel = () => {
      visible.value = false;
      resetSelection();
    };
    const handleSearch = debounce(() => {
      currentPage.value = 1;
      fetchTools();
    }, 500);
    const handlePageChange = (page) => {
      currentPage.value = page;
      fetchTools();
    };
    const handlePageSizeChange = (current, size) => {
      currentPage.value = 1;
      pageSize2.value = size;
      fetchTools();
    };
    const handleRowClick = async (selectedTool) => {
      try {
        loading.value = true;
        const response = await AddTool(wocode, selectedTool);
        if (response?.IsSuccess) {
          emit("tool-added");
          visible.value = false;
          resetSelection();
        } else {
          console.error("Failed to add tool:", response);
        }
      } catch (error) {
        console.error("Error adding tool:", error);
      } finally {
        loading.value = false;
      }
    };
    const resetSelection = () => {
      searchText.value = "";
      currentPage.value = 1;
    };
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_modal = resolveComponent("a-modal");
      const _component_a_input = resolveComponent("a-input");
      const _component_a_table = resolveComponent("a-table");
      _push(ssrRenderComponent(_component_a_modal, mergeProps({
        open: visible.value,
        "onUpdate:open": ($event) => visible.value = $event,
        title: "Chọn thiết bị",
        width: "1200px",
        footer: null,
        bodyStyle: { overflow: "hidden" },
        onCancel: handleCancel
      }, _attrs), {
        default: withCtx((_, _push2, _parent2, _scopeId) => {
          if (_push2) {
            _push2(`<div class="tool-selection-modal" data-v-ce3fc4d8${_scopeId}><div class="mb-4 flex justify-end" data-v-ce3fc4d8${_scopeId}>`);
            _push2(ssrRenderComponent(_component_a_input, {
              value: searchText.value,
              "onUpdate:value": ($event) => searchText.value = $event,
              placeholder: "Tìm kiếm thiết bị...",
              "allow-clear": "",
              onInput: unref(handleSearch),
              style: { "width": "30%" }
            }, {
              suffix: withCtx((_2, _push3, _parent3, _scopeId2) => {
                if (_push3) {
                  _push3(ssrRenderComponent(unref(SearchOutlined), null, null, _parent3, _scopeId2));
                } else {
                  return [
                    createVNode(unref(SearchOutlined))
                  ];
                }
              }),
              _: 1
            }, _parent2, _scopeId));
            _push2(`</div>`);
            _push2(ssrRenderComponent(_component_a_table, {
              columns,
              "data-source": toolsData.value,
              loading: loading.value,
              pagination: {
                current: currentPage.value,
                pageSize: pageSize2.value,
                total: totalItems.value,
                showSizeChanger: true,
                showQuickJumper: true,
                showTotal: (total, range) => `${range[0]}-${range[1]} của ${total} thiết bị`,
                onChange: handlePageChange,
                onShowSizeChange: handlePageSizeChange
              },
              size: "small",
              class: "tools-table",
              scroll: { y: 400 },
              customRow: (record) => ({
                onClick: () => handleRowClick(record)
              })
            }, null, _parent2, _scopeId));
            _push2(`</div>`);
          } else {
            return [
              createVNode("div", { class: "tool-selection-modal" }, [
                createVNode("div", { class: "mb-4 flex justify-end" }, [
                  createVNode(_component_a_input, {
                    value: searchText.value,
                    "onUpdate:value": ($event) => searchText.value = $event,
                    placeholder: "Tìm kiếm thiết bị...",
                    "allow-clear": "",
                    onInput: unref(handleSearch),
                    style: { "width": "30%" }
                  }, {
                    suffix: withCtx(() => [
                      createVNode(unref(SearchOutlined))
                    ]),
                    _: 1
                  }, 8, ["value", "onUpdate:value", "onInput"])
                ]),
                createVNode(_component_a_table, {
                  columns,
                  "data-source": toolsData.value,
                  loading: loading.value,
                  pagination: {
                    current: currentPage.value,
                    pageSize: pageSize2.value,
                    total: totalItems.value,
                    showSizeChanger: true,
                    showQuickJumper: true,
                    showTotal: (total, range) => `${range[0]}-${range[1]} của ${total} thiết bị`,
                    onChange: handlePageChange,
                    onShowSizeChange: handlePageSizeChange
                  },
                  size: "small",
                  class: "tools-table",
                  scroll: { y: 400 },
                  customRow: (record) => ({
                    onClick: () => handleRowClick(record)
                  })
                }, null, 8, ["data-source", "loading", "pagination", "customRow"])
              ])
            ];
          }
        }),
        _: 1
      }, _parent));
    };
  }
});
const _sfc_setup$2 = _sfc_main$2.setup;
_sfc_main$2.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/ToolSelectionModal.vue");
  return _sfc_setup$2 ? _sfc_setup$2(props, ctx) : void 0;
};
const ToolSelectionModal = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$2, [["__scopeId", "data-v-ce3fc4d8"]]), { __name: "ToolSelectionModal" });
const GetWODataListORA = async (wo) => {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.get("TablePerson/LoadPerson", {
      params: {
        wo
      }
    });
    return response.data;
  } catch (error) {
    console.error("Error getting WO data list ORA:", error);
    throw error;
  }
};
const SavePersonTable = async (wocode, data) => {
  try {
    const { $axios } = useNuxtApp();
    const formattedData = data.map((item) => ({
      idRow: item.idRow,
      ViTri: item.ViTri || "",
      ChucDanh: item.ChucDanh || "",
      HoTen: item.HoTen || "",
      MaPer: item.MaPer || "",
      ChuKy: item.ChuKy || "",
      Ngay: item.Ngay || null,
      // Ensure null instead of empty string
      status: item.status || 0
    }));
    const response = await $axios.put("TablePerson/SavePersonTable", formattedData, {
      params: {
        wocode
      }
    });
    return response.data;
  } catch (error) {
    console.error("Error saving person table:", error);
    throw error;
  }
};
const UpdateGhiChuR5 = async (woCode, ghichu) => {
  try {
    const { $axios } = useNuxtApp();
    const response = await $axios.put("Note/UpdateNote", null, {
      params: {
        woCode,
        note: ghichu
      },
      __skipWoInject: true
    });
    return response.data;
  } catch (error) {
    console.error("Error updating ghi chu R5:", error);
    throw error;
  }
};
const nguoiChoPhepPageSize = 50;
const _sfc_main$1 = /* @__PURE__ */ defineComponent({
  __name: "GhiChuWOPersonTable",
  __ssrInlineRender: true,
  props: {
    wocode: {},
    gian: {},
    bophan: {},
    disabled: { type: Boolean, default: false },
    initialGhiChu: { default: "" }
  },
  emits: ["notification"],
  setup(__props, { expose: __expose, emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const ghiChuDeXuat = ref(props.initialGhiChu);
    const resolveWoCodeForRequest = () => {
      return String(props.wocode || "").trim();
    };
    watch(
      () => props.initialGhiChu,
      (newValue) => {
        console.log("initialGhiChu prop changed:", newValue);
        ghiChuDeXuat.value = newValue || "";
      },
      { immediate: true }
    );
    const woPersonData = ref([]);
    const woPersonLoading = ref(false);
    const woPersonOptions = ref(/* @__PURE__ */ new Map());
    const woPersonLoadingStates = ref(/* @__PURE__ */ new Map());
    const woPersonSearchValues = ref(/* @__PURE__ */ new Map());
    const nguoiChoPhepOptions = ref([]);
    const nguoiChoPhepLoaded = ref(false);
    const nguoiChoPhepStartRow = ref(1);
    const nguoiChoPhepHasMore = ref(true);
    const nguoiChoPhepLoadingMore = ref(false);
    const woPersonColumns = [
      {
        title: "",
        dataIndex: "ViTri",
        key: "ViTri",
        width: 200,
        align: "left",
        customRender: ({ record }) => {
          if (record.ViTri === "KS Đ-TĐH") return "KS Đ-TĐH";
          if (record.ViTri === "CV Đ-TĐH") return "CV Đ-TĐH";
          if (record.ViTri === "KST CK") return "KST CK";
          if (record.ViTri === "Gian trưởng") return "Gian trưởng";
          return record.ViTri;
        }
      },
      {
        title: "Chức danh / Должность",
        dataIndex: "ChucDanh",
        key: "ChucDanh",
        width: 200,
        align: "center"
      },
      {
        title: "Họ Tên / Ф.И.О",
        dataIndex: "HoTen",
        key: "HoTen",
        width: 200,
        align: "center"
      },
      {
        title: "Chữ ký / Подпись",
        dataIndex: "ChuKy",
        key: "ChuKy",
        width: 150,
        align: "center"
      },
      {
        title: "Ngày / дата",
        dataIndex: "Ngay",
        key: "Ngay",
        width: 150,
        align: "center"
      }
    ];
    const handleGhiChuBlur = async () => {
      if (props.disabled) return;
      const woCode = resolveWoCodeForRequest();
      if (!woCode) {
        emit("notification", "Thiếu woCode để cập nhật ghi chú!");
        return;
      }
      try {
        const response = await UpdateGhiChuR5(woCode, ghiChuDeXuat.value || "");
        if (response?.IsSuccess) {
          emit("notification", "Cập nhật ghi chú thành công!");
        }
      } catch (error) {
        console.error("Error updating ghi chú:", error);
        emit("notification", "Lỗi khi cập nhật ghi chú!");
      }
    };
    const fetchWOPersonData = async () => {
      const woCode = resolveWoCodeForRequest();
      if (!woCode) return;
      try {
        woPersonLoading.value = true;
        const response = await GetWODataListORA(woCode);
        if (response?.IsSuccess && response.Data) {
          woPersonData.value = response.Data;
          if (props.gian) {
            await loadPersonOptionsForWOCell(0);
          }
        }
      } catch (error) {
        console.error("Error fetching WO person data:", error);
      } finally {
        woPersonLoading.value = false;
      }
    };
    const loadPersonOptionsForWOCell = async (rowIndex) => {
      if (props.disabled) return;
      try {
        woPersonLoadingStates.value.set(rowIndex, true);
        if (props.gian) {
          if (!nguoiChoPhepLoaded.value) {
            await loadNguoiChoPhepFirstPage();
          }
          woPersonOptions.value.set(rowIndex, nguoiChoPhepOptions.value);
          for (let i = 0; i < woPersonData.value.length; i++) {
            if (!woPersonOptions.value.get(i)) {
              woPersonOptions.value.set(i, nguoiChoPhepOptions.value);
            }
          }
        }
      } catch (error) {
        console.error("Error loading person options for WO cell:", error);
      } finally {
        woPersonLoadingStates.value.set(rowIndex, false);
      }
    };
    const mergeUniqueByCode = (current, incoming) => {
      const map = /* @__PURE__ */ new Map();
      for (const item of current) map.set(item.per_code, item);
      for (const item of incoming) map.set(item.per_code, item);
      return Array.from(map.values());
    };
    const fetchNguoiChoPhepPage = async (org, startrowValue, append) => {
      const response = await GetNguoiChoPhep(org, startrowValue);
      const raw = response?.data || [];
      const mapped = raw.map((item) => ({
        per_code: item.schedgroup || item.userdefinedfieldvalue,
        per_desc: stripJobTitle(item.schedgroupdesc || item.description),
        per_jobtitle: (item.JobTitle?.trim() || parseJobTitle(item.schedgroupdesc || item.description) || "").trim()
      }));
      if (append) {
        const before = nguoiChoPhepOptions.value;
        const merged = mergeUniqueByCode(before, mapped);
        const added = merged.length - before.length;
        nguoiChoPhepOptions.value = merged;
        nguoiChoPhepHasMore.value = added > 0 && mapped.length > 0;
        nguoiChoPhepLoaded.value = true;
        return added;
      }
      nguoiChoPhepOptions.value = mapped;
      nguoiChoPhepHasMore.value = mapped.length > 0;
      nguoiChoPhepLoaded.value = true;
      return mapped.length;
    };
    const loadNguoiChoPhepFirstPage = async () => {
      if (!props.gian) return;
      nguoiChoPhepStartRow.value = 1;
      nguoiChoPhepHasMore.value = true;
      nguoiChoPhepOptions.value = [];
      await fetchNguoiChoPhepPage(props.gian, nguoiChoPhepStartRow.value, false);
    };
    const loadNguoiChoPhepMore = async () => {
      if (!props.gian) return;
      if (nguoiChoPhepLoadingMore.value) return;
      if (!nguoiChoPhepHasMore.value) return;
      nguoiChoPhepLoadingMore.value = true;
      try {
        const nextStartRow = nguoiChoPhepStartRow.value + nguoiChoPhepPageSize;
        const loadedCount = await fetchNguoiChoPhepPage(props.gian, nextStartRow, true);
        if (loadedCount > 0) {
          nguoiChoPhepStartRow.value = nextStartRow;
        }
      } finally {
        nguoiChoPhepLoadingMore.value = false;
      }
    };
    const handleNguoiChoPhepPopupScroll = async (e) => {
      let el = e?.target;
      while (el && el.scrollHeight <= el.clientHeight && el.parentElement) {
        el = el.parentElement;
      }
      if (!el) return;
      const threshold = 64;
      if (el.scrollTop + el.clientHeight + threshold >= el.scrollHeight) {
        await loadNguoiChoPhepMore();
      }
    };
    const handlePersonSearchInWOCell = (value, rowIndex) => {
      if (props.disabled) return;
      woPersonSearchValues.value.set(rowIndex, value);
    };
    const handlePersonSelectInWOCell = async (value, record, rowIndex) => {
      if (props.disabled) return;
      const woCode = resolveWoCodeForRequest();
      if (!woCode) {
        emit("notification", "Thiếu woCode để cập nhật người!");
        return;
      }
      try {
        const options = woPersonOptions.value.get(rowIndex) || [];
        const selectedPerson = options.find((person) => person.per_code === value);
        if (selectedPerson) {
          record.HoTen = selectedPerson.per_desc;
          record.ChucDanh = selectedPerson.per_jobtitle || "";
          record.MaPer = selectedPerson.per_code;
        }
        record.status = 1;
        const updateData = woPersonData.value.map((item) => ({
          idRow: item.idRow,
          ViTri: item.ViTri,
          ChucDanh: item.ChucDanh || "",
          HoTen: item.HoTen || "",
          MaPer: item.MaPer || "",
          ChuKy: item.ChuKy || "",
          Ngay: item.Ngay || "",
          status: item.status || 0
        }));
        await SavePersonTable(woCode, updateData);
        emit("notification", "Cập nhật người thành công!");
      } catch (error) {
        console.error("Error selecting person in WO cell:", error);
        emit("notification", "Lỗi khi cập nhật người!");
      }
    };
    const getPersonOptionsForWORow = (rowIndex) => {
      const options = nguoiChoPhepLoaded.value ? nguoiChoPhepOptions.value : woPersonOptions.value.get(rowIndex) || [];
      const searchValue = woPersonSearchValues.value.get(rowIndex) || "";
      if (!searchValue) return options;
      return options.filter(
        (person) => person.per_desc?.toLowerCase().includes(searchValue.toLowerCase())
      );
    };
    const parseJobTitle = (desc) => {
      const match = desc?.match(/\(([^)]+)\)/);
      return match && match[1] ? match[1].trim() : "";
    };
    const stripJobTitle = (desc) => {
      return (desc || "").replace(/\s*\([^)]*\)\s*$/, "").trim();
    };
    const formatViTri = (text) => {
      return (text || "").replace(/\r\n|\n|\r/g, "<br/>");
    };
    const formatNgay = (value) => {
      const raw = (value || "").toString();
      if (!raw) return [""];
      const toDateMonthYear = (input) => {
        const trimmed = input.trim();
        if (!trimmed) return "";
        if (/^\d{2}\/\d{2}\/\d{4}$/.test(trimmed)) {
          return trimmed;
        }
        const date = new Date(trimmed);
        if (!Number.isNaN(date.getTime())) {
          const dd = String(date.getDate()).padStart(2, "0");
          const mm = String(date.getMonth() + 1).padStart(2, "0");
          const yyyy = date.getFullYear();
          return `${dd}/${mm}/${yyyy}`;
        }
        return trimmed;
      };
      const parts = raw.split(/<br\s*\/?>|\r\n|\n|\r/gi).map((p) => p.trim()).filter(Boolean);
      const formatted = parts.map(toDateMonthYear);
      return formatted.length ? formatted : [""];
    };
    __expose({
      fetchWOPersonData,
      ghiChuDeXuat
    });
    watch(
      () => props.gian,
      () => {
        nguoiChoPhepLoaded.value = false;
        nguoiChoPhepOptions.value = [];
        nguoiChoPhepStartRow.value = 1;
        nguoiChoPhepHasMore.value = true;
      }
    );
    return (_ctx, _push, _parent, _attrs) => {
      const _component_a_textarea = resolveComponent("a-textarea");
      const _component_a_table = resolveComponent("a-table");
      const _component_a_select = resolveComponent("a-select");
      const _component_a_select_option = resolveComponent("a-select-option");
      _push(`<div${ssrRenderAttrs(_attrs)} data-v-97ec1a99><div class="w-full mt-6" data-v-97ec1a99><div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm" data-v-97ec1a99> 9. Ghi chú, đề xuất, kiến nghị / Примечание: </div><div class="mt-4" data-v-97ec1a99><div data-v-97ec1a99>`);
      _push(ssrRenderComponent(_component_a_textarea, {
        value: ghiChuDeXuat.value,
        "onUpdate:value": ($event) => ghiChuDeXuat.value = $event,
        rows: 6,
        placeholder: "Nhập ghi chú, đề xuất, kiến nghị...",
        onBlur: handleGhiChuBlur,
        class: "w-full",
        "show-count": "",
        disabled: __props.disabled
      }, null, _parent));
      _push(`</div></div></div><div class="w-full mt-6" data-v-97ec1a99><div class="mt-4" data-v-97ec1a99>`);
      _push(ssrRenderComponent(_component_a_table, {
        columns: woPersonColumns,
        "data-source": woPersonData.value,
        loading: woPersonLoading.value,
        pagination: false,
        scroll: { x: 900 },
        class: "wo-person-table",
        size: "small",
        bordered: ""
      }, {
        bodyCell: withCtx(({ column, record, index }, _push2, _parent2, _scopeId) => {
          if (_push2) {
            if (column.dataIndex === "ViTri") {
              _push2(`<div class="text-left font-medium" data-v-97ec1a99${_scopeId}>${formatViTri(record.ViTri) ?? ""}</div>`);
            } else if (column.dataIndex === "ChucDanh") {
              _push2(`<div class="text-center" data-v-97ec1a99${_scopeId}>${ssrInterpolate(record.ChucDanh)}</div>`);
            } else if (column.dataIndex === "HoTen") {
              _push2(`<div class="text-center" data-v-97ec1a99${_scopeId}>`);
              _push2(ssrRenderComponent(_component_a_select, {
                value: record.MaPer,
                "onUpdate:value": ($event) => record.MaPer = $event,
                "show-search": "",
                placeholder: "Chọn người",
                "filter-option": false,
                loading: woPersonLoadingStates.value.get(index) || false,
                onFocus: () => loadPersonOptionsForWOCell(index),
                onSearch: (value) => handlePersonSearchInWOCell(value, index),
                onSelect: ($event) => handlePersonSelectInWOCell($event, record, index),
                onPopupScroll: [handleNguoiChoPhepPopupScroll, handleNguoiChoPhepPopupScroll],
                class: "w-full",
                size: "small",
                disabled: __props.disabled
              }, {
                default: withCtx((_, _push3, _parent3, _scopeId2) => {
                  if (_push3) {
                    if (!woPersonOptions.value.get(index)?.length && record.MaPer && record.HoTen) {
                      _push3(ssrRenderComponent(_component_a_select_option, {
                        key: record.MaPer,
                        value: record.MaPer
                      }, {
                        default: withCtx((_2, _push4, _parent4, _scopeId3) => {
                          if (_push4) {
                            _push4(`<div class="flex flex-col" data-v-97ec1a99${_scopeId3}><span class="font-medium" data-v-97ec1a99${_scopeId3}>${ssrInterpolate(record.HoTen)}</span><span class="text-xs text-gray-500" data-v-97ec1a99${_scopeId3}>${ssrInterpolate(record.ChucDanh)}</span></div>`);
                          } else {
                            return [
                              createVNode("div", { class: "flex flex-col" }, [
                                createVNode("span", { class: "font-medium" }, toDisplayString(record.HoTen), 1),
                                createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(record.ChucDanh), 1)
                              ])
                            ];
                          }
                        }),
                        _: 2
                      }, _parent3, _scopeId2));
                    } else {
                      _push3(`<!---->`);
                    }
                    _push3(`<!--[-->`);
                    ssrRenderList(getPersonOptionsForWORow(index), (person) => {
                      _push3(ssrRenderComponent(_component_a_select_option, {
                        key: person.per_code,
                        value: person.per_code
                      }, {
                        default: withCtx((_2, _push4, _parent4, _scopeId3) => {
                          if (_push4) {
                            _push4(`<div class="flex flex-col" data-v-97ec1a99${_scopeId3}><span class="font-medium" data-v-97ec1a99${_scopeId3}>${ssrInterpolate(person.per_desc)}</span><span class="text-xs text-gray-500" data-v-97ec1a99${_scopeId3}>${ssrInterpolate(person.per_jobtitle)}</span></div>`);
                          } else {
                            return [
                              createVNode("div", { class: "flex flex-col" }, [
                                createVNode("span", { class: "font-medium" }, toDisplayString(person.per_desc), 1),
                                createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(person.per_jobtitle), 1)
                              ])
                            ];
                          }
                        }),
                        _: 2
                      }, _parent3, _scopeId2));
                    });
                    _push3(`<!--]-->`);
                  } else {
                    return [
                      !woPersonOptions.value.get(index)?.length && record.MaPer && record.HoTen ? (openBlock(), createBlock(_component_a_select_option, {
                        key: record.MaPer,
                        value: record.MaPer
                      }, {
                        default: withCtx(() => [
                          createVNode("div", { class: "flex flex-col" }, [
                            createVNode("span", { class: "font-medium" }, toDisplayString(record.HoTen), 1),
                            createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(record.ChucDanh), 1)
                          ])
                        ]),
                        _: 2
                      }, 1032, ["value"])) : createCommentVNode("", true),
                      (openBlock(true), createBlock(Fragment, null, renderList(getPersonOptionsForWORow(index), (person) => {
                        return openBlock(), createBlock(_component_a_select_option, {
                          key: person.per_code,
                          value: person.per_code
                        }, {
                          default: withCtx(() => [
                            createVNode("div", { class: "flex flex-col" }, [
                              createVNode("span", { class: "font-medium" }, toDisplayString(person.per_desc), 1),
                              createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(person.per_jobtitle), 1)
                            ])
                          ]),
                          _: 2
                        }, 1032, ["value"]);
                      }), 128))
                    ];
                  }
                }),
                _: 2
              }, _parent2, _scopeId));
              _push2(`</div>`);
            } else if (column.dataIndex === "ChuKy") {
              _push2(`<div class="text-center" data-v-97ec1a99${_scopeId}>${(record.ChuKy || "") ?? ""}</div>`);
            } else if (column.dataIndex === "Ngay") {
              _push2(`<div class="text-center" data-v-97ec1a99${_scopeId}><!--[-->`);
              ssrRenderList(formatNgay(record.Ngay), (line, i) => {
                _push2(`<div data-v-97ec1a99${_scopeId}>${ssrInterpolate(line)}</div>`);
              });
              _push2(`<!--]--></div>`);
            } else {
              _push2(`<!---->`);
            }
          } else {
            return [
              column.dataIndex === "ViTri" ? (openBlock(), createBlock("div", {
                key: 0,
                class: "text-left font-medium",
                innerHTML: formatViTri(record.ViTri)
              }, null, 8, ["innerHTML"])) : column.dataIndex === "ChucDanh" ? (openBlock(), createBlock("div", {
                key: 1,
                class: "text-center"
              }, toDisplayString(record.ChucDanh), 1)) : column.dataIndex === "HoTen" ? (openBlock(), createBlock("div", {
                key: 2,
                class: "text-center"
              }, [
                createVNode(_component_a_select, {
                  value: record.MaPer,
                  "onUpdate:value": ($event) => record.MaPer = $event,
                  "show-search": "",
                  placeholder: "Chọn người",
                  "filter-option": false,
                  loading: woPersonLoadingStates.value.get(index) || false,
                  onFocus: () => loadPersonOptionsForWOCell(index),
                  onSearch: (value) => handlePersonSearchInWOCell(value, index),
                  onSelect: ($event) => handlePersonSelectInWOCell($event, record, index),
                  onPopupScroll: [handleNguoiChoPhepPopupScroll, handleNguoiChoPhepPopupScroll],
                  class: "w-full",
                  size: "small",
                  disabled: __props.disabled
                }, {
                  default: withCtx(() => [
                    !woPersonOptions.value.get(index)?.length && record.MaPer && record.HoTen ? (openBlock(), createBlock(_component_a_select_option, {
                      key: record.MaPer,
                      value: record.MaPer
                    }, {
                      default: withCtx(() => [
                        createVNode("div", { class: "flex flex-col" }, [
                          createVNode("span", { class: "font-medium" }, toDisplayString(record.HoTen), 1),
                          createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(record.ChucDanh), 1)
                        ])
                      ]),
                      _: 2
                    }, 1032, ["value"])) : createCommentVNode("", true),
                    (openBlock(true), createBlock(Fragment, null, renderList(getPersonOptionsForWORow(index), (person) => {
                      return openBlock(), createBlock(_component_a_select_option, {
                        key: person.per_code,
                        value: person.per_code
                      }, {
                        default: withCtx(() => [
                          createVNode("div", { class: "flex flex-col" }, [
                            createVNode("span", { class: "font-medium" }, toDisplayString(person.per_desc), 1),
                            createVNode("span", { class: "text-xs text-gray-500" }, toDisplayString(person.per_jobtitle), 1)
                          ])
                        ]),
                        _: 2
                      }, 1032, ["value"]);
                    }), 128))
                  ]),
                  _: 2
                }, 1032, ["value", "onUpdate:value", "loading", "onFocus", "onSearch", "onSelect", "disabled"])
              ])) : column.dataIndex === "ChuKy" ? (openBlock(), createBlock("div", {
                key: 3,
                class: "text-center",
                innerHTML: record.ChuKy || ""
              }, null, 8, ["innerHTML"])) : column.dataIndex === "Ngay" ? (openBlock(), createBlock("div", {
                key: 4,
                class: "text-center"
              }, [
                (openBlock(true), createBlock(Fragment, null, renderList(formatNgay(record.Ngay), (line, i) => {
                  return openBlock(), createBlock("div", { key: i }, toDisplayString(line), 1);
                }), 128))
              ])) : createCommentVNode("", true)
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div></div></div>`);
    };
  }
});
const _sfc_setup$1 = _sfc_main$1.setup;
_sfc_main$1.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/GhiChuWOPersonTable.vue");
  return _sfc_setup$1 ? _sfc_setup$1(props, ctx) : void 0;
};
const GhiChuWOPersonTable = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$1, [["__scopeId", "data-v-97ec1a99"]]), { __name: "GhiChuWOPersonTable" });
const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "FORMMAIN",
  __ssrInlineRender: true,
  setup(__props) {
    const route = useRoute();
    const resolveWoCode2 = () => {
      const fromRoute = route.query.wocode || route.query.wo || route.query.WO;
      const normalized = Array.isArray(fromRoute) ? fromRoute[0] : fromRoute;
      if (normalized) return String(normalized).trim();
      const requestUrl = useRequestURL();
      const fromRequest = (requestUrl.searchParams.get("wocode") || "").trim() || (requestUrl.searchParams.get("wo") || "").trim() || (requestUrl.searchParams.get("WO") || "").trim();
      if (fromRequest) return fromRequest;
      return "";
    };
    const wocode = resolveWoCode2();
    const data = ref(null);
    const tinhTrangThietBi = ref(null);
    const tongThoiGian = ref("");
    const nhanLuc = ref("");
    const pending = ref(true);
    const error = ref(null);
    const congViecKhacChecked = ref(false);
    const congViecKhacText = ref("");
    const congViecKhacLine = ref(0);
    const congViecKhacType = ref("");
    const showNotification = ref(false);
    const ndTruocData = ref(null);
    const toolData = ref([]);
    const toolLoading = ref(false);
    const showToolModal = ref(false);
    const designerGroups = ref([]);
    const designerLoading = ref(false);
    const designerError = ref(null);
    const dynamicHeaderByBoPhan = ref([]);
    const optionValuesByType = ref({});
    ref({});
    ref({});
    const ndSauData = ref(null);
    const ndSauChecked = ref(false);
    const ndSauText = ref("");
    const ndSauLine = ref(0);
    const ndSauType = ref("");
    const ndBaoDuongData = ref(null);
    const ndBaoDuongChecked = ref(false);
    const ndBaoDuongText = ref("");
    const ndBaoDuongLine = ref(0);
    const ndBaoDuongType = ref("");
    const timerValue = ref(void 0);
    const ptwNoValue = ref("");
    const handleNotificationFromComponent = (message2) => {
      notificationMessage.value = message2;
      showNotification.value = true;
    };
    const notificationMessage = ref("Cập nhật thành công!");
    const isExporting = ref(false);
    const applyFirstFormData = (newData) => {
      data.value = newData;
      if (data.value?.TinhTrangTbi) {
        let mappedValue = null;
        const tinhTrangText = data.value.TinhTrangTbi.toString().trim();
        if (tinhTrangText.includes("Tốt, sẵn sàng hoạt động") || tinhTrangText === "1") {
          mappedValue = 1;
        } else if (tinhTrangText.includes("Có thể hoạt động tạm thời") || tinhTrangText === "2") {
          mappedValue = 2;
        } else if (tinhTrangText.includes("Cần sửa chữa, cấm hoạt động") || tinhTrangText === "3") {
          mappedValue = 3;
        } else {
          const parsedValue = parseInt(tinhTrangText);
          if (!isNaN(parsedValue) && parsedValue >= 1 && parsedValue <= 3) {
            mappedValue = parsedValue;
          }
        }
        if (mappedValue !== null) {
          tinhTrangThietBi.value = mappedValue;
        }
      }
      const t = (data.value?.Timer ?? "").toString().trim();
      const parsed = parseFloat(t);
      timerValue.value = !isNaN(parsed) ? parsed : void 0;
      ptwNoValue.value = (data.value?.PTWNo ?? "").toString().trim();
    };
    const refreshFirstForm = async () => {
      try {
        const response = await GetFirstForm(wocode);
        if (response?.IsSuccess) {
          applyFirstFormData(response.Data);
        }
      } catch (err) {
        console.error("Error refreshing first form data:", err);
      }
    };
    watch(
      () => String(data.value?.bophan || "").trim().toUpperCase(),
      async (newValue, oldValue) => {
        if (newValue === oldValue) return;
        await loadDynamicHeaderByBoPhan();
      }
    );
    const getSortedNodes = (nodes) => {
      return [...nodes || []].sort((a, b) => Number(a?.displayOrder || 0) - Number(b?.displayOrder || 0));
    };
    const getNodeDepth = (node) => {
      const children = getSortedNodes(node.children || []);
      if (!children.length) return 1;
      return 1 + Math.max(...children.map(getNodeDepth));
    };
    const countLeafNodes = (node) => {
      const children = getSortedNodes(node.children || []);
      if (!children.length) return 1;
      return children.reduce((sum, child) => sum + countLeafNodes(child), 0);
    };
    const buildHeaderPreviewRows = (nodes) => {
      const sortedRoots = getSortedNodes(nodes);
      if (!sortedRoots.length) return [];
      const maxDepth = Math.max(...sortedRoots.map(getNodeDepth));
      const rows = Array.from({ length: maxDepth }, () => []);
      const visit = (node, depth) => {
        const children = getSortedNodes(node.children || []);
        const colspan = countLeafNodes(node);
        const rowspan = children.length === 0 ? maxDepth - depth + 1 : 1;
        rows[depth - 1]?.push({ node, colspan, rowspan });
        children.forEach((child) => visit(child, depth + 1));
      };
      sortedRoots.forEach((root) => visit(root, 1));
      return rows;
    };
    const collectLeafNodes = (nodes) => {
      const leaves = [];
      const traverse = (node) => {
        const children = getSortedNodes(node.children || []);
        if (!children.length) {
          leaves.push(node);
          return;
        }
        children.forEach(traverse);
      };
      getSortedNodes(nodes).forEach(traverse);
      return leaves;
    };
    const normalizeKey = (value) => value.replace(/_/g, "").toUpperCase();
    const isUdfField = (sourceField) => /^UDF([1-9]|1[0-5])$/i.test(String(sourceField || "").trim());
    const READONLY_SOURCE_FIELDS = /* @__PURE__ */ new Set(["NO", "TAGNAME", "DESCRIPTION", "LOCATION"]);
    const getItemValueBySourceField = (item, sourceField) => {
      const source = String(sourceField || "").trim();
      if (!source) return "";
      const targetKey = normalizeKey(source);
      const key = Object.keys(item).find((k) => normalizeKey(k) === targetKey);
      if (!key) return "";
      const value = item[key];
      if (value === null || value === void 0) return "";
      return String(value);
    };
    const getRawGroupUdfConfigs = (group) => {
      return Array.isArray(group.udfFieldConfigs) ? group.udfFieldConfigs : [];
    };
    const getRawGroupFieldStyles = (group) => {
      if (Array.isArray(group.fieldStyles)) return group.fieldStyles;
      if (Array.isArray(group.FieldStyles)) return group.FieldStyles;
      return [];
    };
    const normalizeOptionSelectionMode = (row) => {
      const rawMode = row?.optionSelectionMode ?? row?.OptionSelectionMode ?? row?.option_selection_mode ?? row?.OPTION_SELECTION_MODE ?? row?.Option_Selection_Mode ?? "";
      return String(rawMode).trim().toLowerCase() === "multiple" ? "multiple" : "single";
    };
    const normalizeFieldStyleConfig = (row) => ({
      sourceField: String(row?.sourceField ?? row?.SourceField ?? "").trim().toUpperCase(),
      headerText: String(row?.headerText ?? row?.HeaderText ?? "").trim() || void 0,
      headerStyle: row?.headerStyle ?? row?.HeaderStyle ?? null,
      valueStyle: row?.valueStyle ?? row?.ValueStyle ?? null,
      dataType: row?.dataType ?? row?.DataType ?? null,
      inputType: row?.inputType ?? row?.InputType ?? null,
      optionalType: row?.optionalType ?? row?.OptionalType ?? null ? String(row?.optionalType ?? row?.OptionalType).trim().toUpperCase() : null,
      optionSelectionMode: normalizeOptionSelectionMode(row)
    });
    const normalizeAlign = (align) => {
      const normalized = String(align || "").trim().toLowerCase();
      if (normalized === "left" || normalized === "right") return normalized;
      return "center";
    };
    const normalizeUdfConfig = (row) => ({
      sourceField: String(row?.sourceField ?? row?.SourceField ?? "").trim().toUpperCase(),
      headerText: String(row?.headerText ?? row?.HeaderText ?? "").trim(),
      dataType: String(row?.dataType ?? row?.DataType ?? "text").toLowerCase(),
      optionalType: row?.optionalType ?? row?.OptionalType ?? null ? String(row?.optionalType ?? row?.OptionalType).trim().toUpperCase() : null,
      inputType: row?.inputType ?? row?.InputType ?? null ? String(row?.inputType ?? row?.InputType).trim().toLowerCase() : null,
      optionSelectionMode: normalizeOptionSelectionMode(row)
    });
    const getUdfConfig = (group, sourceField) => {
      if (!isUdfField(sourceField)) return null;
      const source = String(sourceField || "").trim().toUpperCase();
      const rows = getRawGroupUdfConfigs(group);
      const found = rows.find((row) => String(row?.sourceField ?? row?.SourceField ?? "").trim().toUpperCase() === source);
      return found ? normalizeUdfConfig(found) : null;
    };
    const getFieldStyleConfig = (group, sourceField) => {
      const source = String(sourceField || "").trim().toUpperCase();
      if (!source) return null;
      const rows = getRawGroupFieldStyles(group);
      const found = rows.find((row) => String(row?.sourceField ?? row?.SourceField ?? "").trim().toUpperCase() === source);
      return found ? normalizeFieldStyleConfig(found) : null;
    };
    const getCellEditorType = (group, leaf) => {
      const sourceField = String(leaf?.sourceField || "").trim().toUpperCase();
      if (!sourceField) return "text";
      if (READONLY_SOURCE_FIELDS.has(sourceField)) return "readonly";
      const fieldStyleConfig = getFieldStyleConfig(group, sourceField);
      if (fieldStyleConfig) {
        const dataType = String(fieldStyleConfig.dataType || "").toLowerCase();
        const inputType = String(fieldStyleConfig.inputType || "").toLowerCase();
        if (dataType === "option" || inputType === "select") return "option";
        if (dataType === "number" || inputType === "number") return "number";
      }
      const udfConfig = getUdfConfig(group, sourceField);
      if (udfConfig) {
        if (udfConfig.dataType === "option" || udfConfig.inputType === "select") return "option";
        if (udfConfig.dataType === "number" || udfConfig.inputType === "number") return "number";
        return "text";
      }
      const leafDataType = String(leaf?.dataType || leaf?.DataType || "").toLowerCase();
      const leafInputType = String(leaf?.inputType || leaf?.InputType || "").toLowerCase();
      if (leafDataType === "option" || leafInputType === "select") return "option";
      if (leafDataType === "number" || leafInputType === "number") return "number";
      return "text";
    };
    const getOptionalTypeForLeaf = (group, leaf) => {
      const sourceField = String(leaf?.sourceField || "").trim().toUpperCase();
      const fieldStyleConfig = getFieldStyleConfig(group, sourceField);
      if (fieldStyleConfig?.optionalType) return fieldStyleConfig.optionalType;
      const udfConfig = getUdfConfig(group, sourceField);
      if (udfConfig?.optionalType) return udfConfig.optionalType;
      const leafOptional = leaf.optionalType ?? leaf.OptionalType;
      return leafOptional ? String(leafOptional).trim().toUpperCase() : null;
    };
    const getOptionValuesForLeaf = (group, leaf) => {
      const optionalType = getOptionalTypeForLeaf(group, leaf);
      if (!optionalType) return [];
      return optionValuesByType.value[optionalType] || [];
    };
    const optionDropdownDirectionMap = ref({});
    const getOptionDropdownKey = (group, itemIndex, leaf) => {
      const source = String(leaf?.sourceField || "").trim().toUpperCase();
      return `${group.type}::${itemIndex}::${source || leaf.headerId || leaf.displayOrder}`;
    };
    const getOptionDropdownPositionClass = (group, itemIndex, leaf) => {
      const key = getOptionDropdownKey(group, itemIndex, leaf);
      const direction = optionDropdownDirectionMap.value[key] || "down";
      if (direction === "up") {
        return "bottom-full mb-1";
      }
      return "top-full mt-1";
    };
    const parseOptionCellValues = (value) => value.split(",").map((item) => item.trim()).filter(Boolean);
    const getSelectedOptionValues = (item, leaf) => {
      const raw = getItemValueBySourceField(item, leaf.sourceField);
      return parseOptionCellValues(raw);
    };
    const isOptionSelected = (item, leaf, optionValue) => {
      const selected = getSelectedOptionValues(item, leaf);
      return selected.includes(optionValue);
    };
    const getSelectedOptionDisplayList = (group, item, leaf) => {
      const selected = getSelectedOptionValues(item, leaf);
      if (!selected.length) return [];
      const available = getOptionValuesForLeaf(group, leaf);
      const ordered = available.filter((opt) => selected.includes(opt) && String(opt).trim());
      const fallback = selected.filter((opt) => !ordered.includes(opt) && String(opt).trim());
      return [...ordered, ...fallback];
    };
    const getAlignClass = (align) => {
      if (align === "left") return "text-left";
      if (align === "right") return "text-right";
      return "text-center";
    };
    const getJustifyClass = (align) => {
      if (align === "right") return "justify-end";
      if (align === "center") return "justify-center";
      return "justify-start";
    };
    const getHeaderCellStyle = (group, node) => {
      const fieldStyleConfig = getFieldStyleConfig(group, node.sourceField);
      const configHeaderStyle = fieldStyleConfig?.headerStyle || {};
      const nodeHeaderStyle = node?.style || {};
      const style = {
        ...nodeHeaderStyle,
        ...configHeaderStyle
      };
      const headerStyle = {};
      if (style?.fontWeight) headerStyle.fontWeight = String(style.fontWeight);
      if (style?.fontSize) headerStyle.fontSize = `${Number(style.fontSize)}px`;
      if (style?.backgroundColor) headerStyle.backgroundColor = String(style.backgroundColor);
      if (style?.border === "none") headerStyle.border = "none";
      return headerStyle;
    };
    const getGroupLeafColumns = (group) => {
      return collectLeafNodes(group?.template?.headerChildren || []);
    };
    const getGroupTotalLeafWidth = (group) => {
      return getGroupLeafColumns(group).reduce((total, leaf) => total + Math.max(1, Number(leaf.width) || 0), 0);
    };
    const getGroupLeafWidthStyle = (group, leaf) => {
      const total = getGroupTotalLeafWidth(group);
      if (!total) return {};
      const normalizedWidth = Math.max(1, Number(leaf.width) || 1);
      return {
        width: `${normalizedWidth / total * 100}%`
      };
    };
    const getValueAlign = (group, leaf) => {
      const fieldStyleConfig = getFieldStyleConfig(group, leaf.sourceField);
      const configValueStyle = fieldStyleConfig?.valueStyle || {};
      const leafValueStyle = leaf?.valueStyle || {};
      return normalizeAlign(configValueStyle?.align ?? leafValueStyle?.align ?? leaf?.align);
    };
    const getValueCellStyle = (group, leaf) => {
      const fieldStyleConfig = getFieldStyleConfig(group, leaf.sourceField);
      const configValueStyle = fieldStyleConfig?.valueStyle || {};
      const leafValueStyle = leaf?.valueStyle || {};
      const valueStyle = {
        ...leafValueStyle,
        ...configValueStyle
      };
      const style = {};
      style.textAlign = getValueAlign(group, leaf);
      if (valueStyle?.fontWeight) style.fontWeight = String(valueStyle.fontWeight);
      if (valueStyle?.fontSize) style.fontSize = `${Number(valueStyle.fontSize)}px`;
      if (valueStyle?.backgroundColor) style.backgroundColor = String(valueStyle.backgroundColor);
      if (valueStyle?.color) style.color = String(valueStyle.color);
      if (valueStyle?.padding !== void 0 && valueStyle?.padding !== null) style.padding = `${Number(valueStyle.padding)}px`;
      if (valueStyle?.border === "none") style.border = "none";
      return style;
    };
    const getHeaderParentAlign = (parent) => {
      const align = String(parent?.style?.align || parent?.style?.textAlign || "center");
      return align === "left" || align === "right" ? align : "center";
    };
    const loadDynamicHeaderByBoPhan = async () => {
      const boPhan = String(data.value?.bophan || "").trim().toUpperCase();
      if (!boPhan) {
        dynamicHeaderByBoPhan.value = [];
        return;
      }
      try {
        dynamicHeaderByBoPhan.value = await GetDynamicFormHeaders(boPhan);
      } catch (error2) {
        console.error("Error fetching dynamic header by bophan:", error2);
        dynamicHeaderByBoPhan.value = [];
      }
    };
    const dynamicHeaderTitleLines = computed(
      () => dynamicHeaderByBoPhan.value.map((item) => String(item.text || "").trim()).filter(Boolean).slice(0, 4)
    );
    const isFormDisabled = computed(() => {
      return data.value?.evtstatus == "C" || data.value?.evtstatus == "R6";
    });
    const handleNotificationHide = () => {
      showNotification.value = false;
    };
    const handleToggleItem = async (item) => {
      try {
        const newIstick = item.ND_ISTICK === "+" ? "-" : "+";
        console.log("Toggling checkbox:", item.ND_Line, "from", item.ND_ISTICK, "to", newIstick);
        const response = await UpdateTichTruocND(wocode, item.ND_Line, newIstick);
        console.log("API response:", response);
        item.ND_ISTICK = newIstick;
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating tick status:", error2);
        showNotification.value = true;
      }
    };
    const openToolModal = () => {
      showToolModal.value = true;
    };
    const handleToolAdded = async () => {
      await fetchToolData();
      showNotification.value = true;
      showToolModal.value = false;
    };
    const fetchToolData = async () => {
      try {
        toolLoading.value = true;
        const response = await GetToolByWO(wocode);
        if (response?.IsSuccess && response.Data) {
          toolData.value = response.Data;
        } else {
          toolData.value = [];
        }
      } catch (err) {
        console.error("Error fetching tool data:", err);
        toolData.value = [];
      } finally {
        toolLoading.value = false;
      }
    };
    const handleDeleteTool = async (tool) => {
      try {
        const response = await DeleteTool(wocode, tool.Ma_Tool);
        if (response?.IsSuccess) {
          const index = toolData.value.findIndex((item) => item.Ma_Tool === tool.Ma_Tool);
          if (index > -1) {
            toolData.value.splice(index, 1);
          }
          showNotification.value = true;
        } else {
          showNotification.value = true;
        }
      } catch (error2) {
        console.error("Error deleting tool:", error2);
        showNotification.value = true;
      }
    };
    const handleCongViecKhacTickUpdate = async (checked) => {
      congViecKhacChecked.value = checked;
      try {
        const istick = checked ? "+" : "-";
        const response = await UpdateTichCVK(wocode, congViecKhacLine.value, istick, congViecKhacType.value);
        if (response?.IsSuccess) {
          notificationMessage.value = "Cập nhật thành công!";
          showNotification.value = true;
        } else {
          notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
          showNotification.value = true;
        }
      } catch (error2) {
        console.error("Error updating công việc khác tick:", error2);
        notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
        showNotification.value = true;
      }
    };
    const handleCongViecKhacTextUpdate = async (text) => {
      congViecKhacText.value = text;
      try {
        await UpdateCVK2(wocode, 1, text);
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating công việc khác text:", error2);
      }
    };
    const handleSauTick = async (item) => {
      try {
        const newIstick = item.ND_ISTICK === "+" ? "-" : "+";
        const response = await TichNdSau(wocode, item.ND_Line, newIstick);
        item.ND_ISTICK = newIstick;
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating tick status Section 6:", error2);
        showNotification.value = true;
      }
    };
    const handleNdSauTickUpdate = async (checked) => {
      ndSauChecked.value = checked;
      try {
        const istick = checked ? "+" : "-";
        const response = await UpdateTichCVK(wocode, ndSauLine.value, istick, ndSauType.value);
        if (response?.IsSuccess) {
          notificationMessage.value = "Cập nhật thành công!";
          showNotification.value = true;
        } else {
          notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
          showNotification.value = true;
        }
      } catch (error2) {
        console.error("Error updating nội dung sau bảo dưỡng tick:", error2);
        notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
        showNotification.value = true;
      }
    };
    const handleNdSauTextUpdate = async (text) => {
      ndSauText.value = text;
      try {
        await NoteCVK6(wocode, text);
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating nội dung sau bảo dưỡng text:", error2);
      }
    };
    const handleBaoDuongTick = async (item) => {
      try {
        const newIstick = item.ND_ISTICK === "+" ? "-" : "+";
        const response = await UpdateTichBaoDuongND(wocode, item.ND_Line, newIstick);
        item.ND_ISTICK = newIstick;
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating bảo dưỡng tick status:", error2);
        showNotification.value = true;
      }
    };
    const handleNdBaoDuongTickUpdate = async (checked) => {
      ndBaoDuongChecked.value = checked;
      try {
        const istick = checked ? "+" : "-";
        const response = await UpdateTichCVK(wocode, ndBaoDuongLine.value, istick, ndBaoDuongType.value);
        if (response?.IsSuccess) {
          notificationMessage.value = "Cập nhật thành công!";
          showNotification.value = true;
        } else {
          notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
          showNotification.value = true;
        }
      } catch (error2) {
        console.error("Error updating nội dung bảo dưỡng tick:", error2);
        notificationMessage.value = "Có lỗi xảy ra khi cập nhật!";
        showNotification.value = true;
      }
    };
    const handleNdBaoDuongTextUpdate = async (text) => {
      ndBaoDuongText.value = text;
      try {
        await NoteCVK4(wocode, text);
        showNotification.value = true;
      } catch (error2) {
        console.error("Error updating nội dung bảo dưỡng text:", error2);
      }
    };
    return (_ctx, _push, _parent, _attrs) => {
      const _component_Loading = __nuxt_component_0;
      const _component_ErrorView = __nuxt_component_1;
      const _component_NotificationToast = __nuxt_component_2;
      _push(`<!--[--><div class="min-h-screen w-full overflow-visible" style="${ssrRenderStyle({ "font-family": "'Times New Roman', Times, serif !important" })}" data-v-233e0533>`);
      _push(ssrRenderComponent(_component_Loading, {
        spinning: pending.value,
        fullscreen: ""
      }, null, _parent));
      if (!pending.value && error.value) {
        _push(ssrRenderComponent(_component_ErrorView, { message: error.value }, null, _parent));
      } else if (!pending.value && data.value) {
        _push(`<div class="bg-white mx-auto" data-v-233e0533><div class="w-[97%] border-2 border-black mx-auto" data-v-233e0533>`);
        _push(ssrRenderComponent(HeaderPermission, {
          data: data.value,
          wocode: unref(wocode),
          disabled: isFormDisabled.value,
          "dynamic-header-lines": dynamicHeaderTitleLines.value,
          onUpdated: refreshFirstForm
        }, null, _parent));
        _push(ssrRenderComponent(CheckboxList, {
          title: "2. Kiểm tra trước khi thực hiện / Проверка перед выполнением",
          data: ndTruocData.value,
          onToggleItem: handleToggleItem,
          showCongViecKhac: true,
          congViecKhacChecked: congViecKhacChecked.value,
          congViecKhacText: congViecKhacText.value,
          isFormDisabled: isFormDisabled.value,
          wocode: unref(wocode),
          onUpdateCongViecKhacTick: handleCongViecKhacTickUpdate,
          onUpdateCongViecKhacText: handleCongViecKhacTextUpdate
        }, null, _parent));
        _push(ssrRenderComponent(ToolEquipmentTable, {
          "tool-data": toolData.value,
          "tool-loading": toolLoading.value,
          onOpenToolModal: openToolModal,
          onDeleteToolItem: handleDeleteTool
        }, null, _parent));
        _push(ssrRenderComponent(CheckboxList, {
          title: "4.Bảo dưỡng hệ thống và thiết bị / Содержание ППР",
          data: ndBaoDuongData.value,
          onToggleItem: handleBaoDuongTick,
          showCongViecKhac: true,
          congViecKhacChecked: ndBaoDuongChecked.value,
          congViecKhacText: ndBaoDuongText.value,
          isFormDisabled: false,
          wocode: unref(wocode),
          onUpdateCongViecKhacTick: handleNdBaoDuongTickUpdate,
          onUpdateCongViecKhacText: handleNdBaoDuongTextUpdate
        }, null, _parent));
        _push(`<div class="font-black bg-blue-200 pl-4 py-3 border border-[#2863c2] rounded-sm shadow-sm mt-2" data-v-233e0533> 5. Kết quả kiểm tra trạng thái làm việc của các thiết bị/ Результаты проверки приборов </div><div class="section-5-container border border-[#2863c2] rounded-sm shadow-sm mt-2 p-3 space-y-4" data-v-233e0533>`);
        if (designerLoading.value) {
          _push(`<div class="text-sm text-slate-600" data-v-233e0533>Đang tải dữ liệu thiết kế theo WO...</div>`);
        } else if (designerError.value) {
          _push(`<div class="text-sm text-sky-700" data-v-233e0533>${ssrInterpolate(designerError.value)}</div>`);
        } else if (!designerGroups.value.length) {
          _push(`<div class="text-sm text-slate-600" data-v-233e0533> There are no records to display. </div>`);
        } else {
          _push(`<div class="space-y-4" data-v-233e0533><!--[-->`);
          ssrRenderList(designerGroups.value, (group) => {
            _push(`<section class="section-5-group rounded border border-slate-300 bg-white" data-v-233e0533><div class="section-5-table-wrap overflow-visible" data-v-233e0533><table class="min-w-full border-collapse table-fixed" data-v-233e0533>`);
            if (getGroupLeafColumns(group).length) {
              _push(`<colgroup data-v-233e0533><!--[-->`);
              ssrRenderList(getGroupLeafColumns(group), (leaf) => {
                _push(`<col style="${ssrRenderStyle(getGroupLeafWidthStyle(group, leaf))}" data-v-233e0533>`);
              });
              _push(`<!--]--></colgroup>`);
            } else {
              _push(`<!---->`);
            }
            _push(`<thead data-v-233e0533><!--[-->`);
            ssrRenderList(group.template?.headerParents?.length ? group.template?.headerParents : [{ text: group.type }], (parent, pIndex) => {
              _push(`<tr data-v-233e0533><th class="${ssrRenderClass([getAlignClass(getHeaderParentAlign(parent)), "border border-slate-300 bg-slate-100 px-2 py-2 text-sm font-semibold"])}"${ssrRenderAttr("colspan", Math.max(getGroupLeafColumns(group).length, 1))} data-v-233e0533>${ssrInterpolate(parent?.text || group.type)}</th></tr>`);
            });
            _push(`<!--]--><!--[-->`);
            ssrRenderList(buildHeaderPreviewRows(group.template?.headerChildren || []), (row, rowIndex) => {
              _push(`<tr data-v-233e0533><!--[-->`);
              ssrRenderList(row, (cell) => {
                _push(`<th class="${ssrRenderClass([getAlignClass(cell.node.align), "border border-slate-300 bg-white px-2 py-2 text-xs font-semibold text-slate-800"])}"${ssrRenderAttr("colspan", cell.colspan)}${ssrRenderAttr("rowspan", cell.rowspan)} style="${ssrRenderStyle(getHeaderCellStyle(group, cell.node))}" data-v-233e0533>${ssrInterpolate(cell.node.text)}</th>`);
              });
              _push(`<!--]--></tr>`);
            });
            _push(`<!--]--></thead><tbody data-v-233e0533>`);
            if (!group.items?.length) {
              _push(`<tr data-v-233e0533><td class="border border-slate-300 px-2 py-3 text-center text-xs text-slate-500"${ssrRenderAttr("colspan", Math.max(getGroupLeafColumns(group).length, 1))} data-v-233e0533> There are no records to display. </td></tr>`);
            } else {
              _push(`<!---->`);
            }
            _push(`<!--[-->`);
            ssrRenderList(group.items, (item, itemIndex) => {
              _push(`<tr data-v-233e0533><!--[-->`);
              ssrRenderList(getGroupLeafColumns(group), (leaf) => {
                _push(`<td class="${ssrRenderClass([getAlignClass(getValueAlign(group, leaf)), "border border-slate-300 px-2 py-1 text-xs text-slate-800"])}" style="${ssrRenderStyle(getValueCellStyle(group, leaf))}" data-v-233e0533>`);
                if (getCellEditorType(group, leaf) === "readonly") {
                  _push(`<span class="${ssrRenderClass([getJustifyClass(getValueAlign(group, leaf)), "flex min-h-[32px] w-full items-center truncate px-1"])}" data-v-233e0533>${ssrInterpolate(getItemValueBySourceField(item, leaf.sourceField))}</span>`);
                } else {
                  _push(`<!---->`);
                }
                if (getCellEditorType(group, leaf) === "option") {
                  _push(`<div class="relative w-full" tabindex="0" data-v-233e0533><details class="group w-full" data-v-233e0533><summary class="block min-h-[32px] w-full cursor-pointer list-none rounded border bg-white px-1.5 py-1 text-xs text-slate-700 outline-none transition group-open:ring-1 group-open:ring-sky-300" style="${ssrRenderStyle({ textAlign: getValueAlign(group, leaf) })}"${ssrRenderAttr("title", getSelectedOptionDisplayList(group, item, leaf).join(", "))} data-v-233e0533><div class="w-full" data-v-233e0533>`);
                  if (!getSelectedOptionDisplayList(group, item, leaf).length) {
                    _push(`<div class="${ssrRenderClass([getJustifyClass(getValueAlign(group, leaf)), "flex min-h-[22px] w-full items-center px-1 py-0.5"])}" data-v-233e0533></div>`);
                  } else {
                    _push(`<div class="${ssrRenderClass([getJustifyClass(getValueAlign(group, leaf)), "flex min-h-[22px] w-full flex-wrap items-start gap-1 px-0.5 py-0.5"])}" data-v-233e0533><!--[-->`);
                    ssrRenderList(getSelectedOptionDisplayList(group, item, leaf), (selected) => {
                      _push(`<div class="inline-flex min-w-0 max-w-full items-center gap-1 rounded border border-slate-300 bg-slate-50 px-1 py-0.5 text-[11px] leading-4" data-v-233e0533><span class="break-all" data-v-233e0533>${ssrInterpolate(selected)}</span><button type="button" class="rounded px-1 text-[10px] text-slate-500 hover:bg-slate-200 hover:text-slate-700" data-v-233e0533> x </button></div>`);
                    });
                    _push(`<!--]--></div>`);
                  }
                  _push(`</div></summary><div class="${ssrRenderClass([getOptionDropdownPositionClass(group, itemIndex, leaf), "absolute left-0 right-0 z-[9999] w-full min-w-full overflow-visible rounded border border-slate-300 bg-white p-1 shadow-lg"])}" data-v-233e0533><!--[-->`);
                  ssrRenderList(getOptionValuesForLeaf(group, leaf), (opt) => {
                    _push(`<button type="button" class="flex w-full items-center justify-between rounded px-2 py-1 text-left text-xs text-slate-700 hover:bg-slate-100" data-v-233e0533><span data-v-233e0533>${ssrInterpolate(opt)}</span>`);
                    if (isOptionSelected(item, leaf, opt)) {
                      _push(`<span class="text-blue-600" data-v-233e0533>✓</span>`);
                    } else {
                      _push(`<!---->`);
                    }
                    _push(`</button>`);
                  });
                  _push(`<!--]-->`);
                  if (!getOptionValuesForLeaf(group, leaf).length) {
                    _push(`<p class="px-1 py-1 text-[11px] text-slate-500" data-v-233e0533> No Data </p>`);
                  } else {
                    _push(`<!---->`);
                  }
                  _push(`</div></details></div>`);
                } else if (getCellEditorType(group, leaf) !== "readonly") {
                  _push(`<input${ssrRenderAttr("type", getCellEditorType(group, leaf) === "number" ? "number" : "text")} class="min-h-[32px] w-full rounded border border-slate-300 px-1 py-0 text-xs outline-none focus:border-blue-600" style="${ssrRenderStyle({ textAlign: getValueAlign(group, leaf) })}"${ssrRenderAttr("value", getItemValueBySourceField(item, leaf.sourceField))} data-v-233e0533>`);
                } else {
                  _push(`<!---->`);
                }
                _push(`</td>`);
              });
              _push(`<!--]-->`);
              if (!getGroupLeafColumns(group).length) {
                _push(`<td class="border border-slate-300 px-2 py-1 text-xs text-slate-500" data-v-233e0533> There are no records to display. </td>`);
              } else {
                _push(`<!---->`);
              }
              _push(`</tr>`);
            });
            _push(`<!--]--></tbody></table></div></section>`);
          });
          _push(`<!--]--></div>`);
        }
        _push(`</div>`);
        _push(ssrRenderComponent(CheckboxList, {
          title: "6.Kiểm tra sau khi bảo dưỡng / Проверка состояния оборудования после ППР",
          data: ndSauData.value,
          onToggleItem: handleSauTick,
          showCongViecKhac: true,
          congViecKhacChecked: ndSauChecked.value,
          congViecKhacText: ndSauText.value,
          isFormDisabled: isFormDisabled.value,
          wocode: unref(wocode),
          onUpdateCongViecKhacTick: handleNdSauTickUpdate,
          onUpdateCongViecKhacText: handleNdSauTextUpdate
        }, null, _parent));
        _push(ssrRenderComponent(TinhTrangThietBiSauBaoDuong, {
          wocode: unref(wocode),
          "initial-tinh-trang-thiet-bi": tinhTrangThietBi.value,
          "initial-tong-thoi-gian": tongThoiGian.value,
          "initial-nhan-luc": nhanLuc.value,
          disabled: isFormDisabled.value,
          onNotification: handleNotificationFromComponent,
          "onUpdate:tinhTrangThietBi": ($event) => tinhTrangThietBi.value = $event,
          "onUpdate:tongThoiGian": ($event) => tongThoiGian.value = $event,
          "onUpdate:nhanLuc": ($event) => nhanLuc.value = $event
        }, null, _parent));
        _push(ssrRenderComponent(VatTuTieuHaoWO, {
          org: data.value?.gian || "",
          wo: data.value?.evt_code || unref(wocode),
          disabled: isFormDisabled.value,
          woequipment: data.value?.matbi || "",
          gian: data.value?.gian || ""
        }, null, _parent));
        _push(ssrRenderComponent(GhiChuWOPersonTable, {
          wocode: unref(wocode),
          initialGhiChu: data.value?.Ghichu || void 0,
          gian: data.value?.gian,
          bophan: data.value?.bophan,
          disabled: isFormDisabled.value,
          onNotification: handleNotificationFromComponent
        }, null, _parent));
        _push(`</div></div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</div><div class="mt-6 flex justify-center" data-v-233e0533><button${ssrIncludeBooleanAttr(isExporting.value || !data.value?.evt_code || !data.value?.matbi) ? " disabled" : ""} class="bg-blue-600 hover:bg-blue-700 disabled:bg-gray-400 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition-colors duration-200 flex items-center gap-2" data-v-233e0533>`);
      if (isExporting.value) {
        _push(`<svg class="animate-spin h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" data-v-233e0533><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" data-v-233e0533></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" data-v-233e0533></path></svg>`);
      } else {
        _push(`<svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" data-v-233e0533><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" data-v-233e0533></path></svg>`);
      }
      _push(` ${ssrInterpolate(isExporting.value ? "Đang xuất PDF..." : "Xuất PDF")}</button></div>`);
      _push(ssrRenderComponent(_component_NotificationToast, {
        show: showNotification.value,
        message: notificationMessage.value,
        duration: 1e3,
        onHide: handleNotificationHide
      }, null, _parent));
      _push(ssrRenderComponent(ToolSelectionModal, {
        visible: showToolModal.value,
        gian: data.value?.gian,
        bophan: data.value?.bophan,
        "onUpdate:visible": ($event) => showToolModal.value = $event,
        onToolAdded: handleToolAdded
      }, null, _parent));
      _push(`<!--]-->`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("pages/FORMMAIN/FORMMAIN.vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const FORMMAIN = /* @__PURE__ */ _export_sfc(_sfc_main, [["__scopeId", "data-v-233e0533"]]);

export { FORMMAIN as default };
//# sourceMappingURL=FORMMAIN-CqbwVUVl.mjs.map
